{
  "name": "Kyra001",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "@cap-js/postgres": "^3.0.1",
    "@sap/cds": "^10",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^3",
    "@sap/cds-dk": "^10",
    "@sap/ux-ui5-tooling": "1",
    "@ui5/cli": "^4.0.33"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "frontend": "npm run start-local --prefix app",
    "start-all": "cds watch & npm run start-local --prefix app"
  },
  "cds": {
    "requires": {
      "html5-repo": true
    }
  },
  "private": true
}
