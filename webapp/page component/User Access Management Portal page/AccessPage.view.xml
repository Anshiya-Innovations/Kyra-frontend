<mvc:View
    controllerName="kyra001.pages.access.AccessPage"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:form="sap.ui.layout.form"
    displayBlock="true">

    <Page id="accessPortalPage" showHeader="false" enableScrolling="true" class="fioriDashboardBg">
        <content>
            <!-- Enterprise Access ShellBar Header -->
            <f:ShellBar
                title="KYRA Enterprise"
                secondTitle="User Access Management Portal"
                homeIcon="sap-icon://shield"
                showCopilot="false"
                showSearch="false"
                showNotifications="false"
                profilePress=".onProfilePress">
                <f:profile>
                    <Avatar id="kyraHeaderProfileAvatar" press=".onProfilePress" src="sap-icon://person-placeholder" class="kyraHeaderProfileAvatar" tooltip="{= 'User Profile: ' + (${accessModel>/activeUser} || 'Dev001') }" />
                </f:profile>
                <f:additionalContent>
                    <!-- Dedicated Notification Bell Button with Blue Badge -->
                    <HBox alignItems="Center" class="kyraNotificationBellWrapper">
                        <Button
                            id="kyraHeaderBellBtn"
                            icon="sap-icon://bell"
                            type="Transparent"
                            press=".onOpenNotificationsPopover"
                            class="kyraHeaderBellBtn"
                            tooltip="Notifications" />
                        <Text
                            text="{accessModel>/notificationsCount}"
                            visible="{= ${accessModel>/notificationsCount} &gt; 0 }"
                            class="kyraHeaderBellBadge" />
                    </HBox>

                    <!-- Header Divider Line -->
                    <core:HTML content="&lt;div class='kyraHeaderDivider'&gt;&lt;/div&gt;" />

                    <Button
                        text="Sign Out"
                        type="Transparent"
                        press=".onLogout"
                        class="kyraSignOutHeaderBtn"
                        tooltip="Sign Out from KYRA Portal" />
                </f:additionalContent>
            </f:ShellBar>

            <!-- Main Content Area Centered with Bottom Scroll Protection -->
            <VBox id="accessMainContentBox" class="fioriMainContentBox fioriBottomPaddingBox">
                <!-- WRAPPER FOR NORMAL PORTAL CONTENT -->
                <VBox id="normalPortalContentWrapper" visible="{= !${accessModel>/showRequestDetailsPage} }">
                    <!-- Enterprise Top Header Banner -->
                    <VBox class="fioriWelcomeCard sapUiSmallMarginBottom sapUiResponsivePadding">
                        <HBox justifyContent="SpaceBetween" alignItems="Center">
                            <HBox alignItems="Center">
                                <!-- Shield Avatar Icon Box -->
                                <Avatar src="sap-icon://shield" backgroundColor="Accent6" displaySize="M" class="sapUiMediumMarginEnd" />
                                <VBox>
                                    <Title text="Enterprise Identity &amp; Access Governance" level="H2" class="fioriWelcomeTitle" />
                                    <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                        <Text text="Active Persona:" class="fioriWelcomeSubtitle sapUiTinyMarginEnd" />
                                        <Text text="{accessModel>/activeRole}" class="kyraHeaderRolePill" />
                                    </HBox>
                                </VBox>
                            </HBox>
                            <HBox alignItems="Center">
                                <HBox alignItems="Center" class="kyraHeaderCompliancePill sapUiMediumMarginEnd">
                                    <core:HTML content="&lt;span class='pulseDotGreen'&gt;&lt;/span&gt;" />
                                    <Text text="100% Active &amp; Compliant" class="kyraComplianceText" />
                                </HBox>
                                <Button
                                    id="fioriHeaderRefreshBtn"
                                    icon="sap-icon://refresh"
                                    text="Refresh"
                                    type="Transparent"
                                    press=".onRefreshAccess"
                                    class="fioriHeaderRefreshBtn"
                                    tooltip="Refresh Access Data" />
                            </HBox>
                        </HBox>
                    </VBox>

                    <!-- SIMPLE SLEEK BUTTONS: MY ACCESS & MY HISTORY -->
                    <HBox visible="{= !${accessModel>/showMyAccessMasterSection} }" class="kyraSectionToggleBar sapUiSmallMarginBottom" alignItems="Center">
                        <Button
                            text="{= 'My Access (' + (${accessModel>/userAccessList} ? ${accessModel>/userAccessList}.length : 0) + ')' }"
                            icon="sap-icon://user-settings"
                            type="{= (${accessModel>/selectedTabKey} || 'myAccess') === 'myAccess' ? 'Emphasized' : 'Transparent' }"
                            press=".onSelectMyAccessTab"
                            class="kyraNavPillBtn sapUiTinyMarginEnd" />
                        <Button
                            text="{= 'My History (' + (${accessModel>/requestHistory} ? ${accessModel>/requestHistory}.length : 0) + ')' }"
                            icon="sap-icon://history"
                            type="{= ${accessModel>/selectedTabKey} === 'myRequests' ? 'Emphasized' : 'Transparent' }"
                            press=".onSelectMyHistoryTab"
                            class="kyraNavPillBtn" />
                    </HBox>

                <!-- SECTION 1: MY ACCESS (First 10 items initially with View All toggle) -->
                <VBox visible="{= !${accessModel>/showMyAccessMasterSection} &amp;&amp; (${accessModel>/selectedTabKey} || 'myAccess') === 'myAccess' }" class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriStepSlideAnim">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                        <HBox alignItems="Center">
                            <Title text="{= (${accessModel>/userAccessList} &amp;&amp; ${accessModel>/userAccessList}.length &gt; 10 &amp;&amp; !${accessModel>/myAccessShowAll}) ? ('Active Entitlements (10 of ' + ${accessModel>/userAccessList}.length + ')') : ('Active Entitlements (' + (${accessModel>/userAccessList} ? ${accessModel>/userAccessList}.length : 0) + ')') }" level="H4" class="fioriCardHeaderTitle" />
                        </HBox>
                        <HBox alignItems="Center">
                            <SearchField
                                placeholder="Search entitlements"
                                width="240px"
                                liveChange=".onSearchMyAccess"
                                class="sapUiTinyMarginEnd" />
                            <Button
                                text="View All"
                                icon="sap-icon://table-view"
                                type="Emphasized"
                                press=".onNavToMyAccessMasterPage"
                                class="kyraViewAllBtn"
                                tooltip="View all active system access and entitlements in complete view" />
                        </HBox>
                    </HBox>

                    <Table
                        id="myAccessTable"
                        items="{accessModel>/displayedUserAccessList}"
                        noDataText="No active permissions found."
                        class="sapUiNoMargin">
                        <columns>
                            <Column width="15%"><Text text="SYSTEM NAME" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="18%"><Text text="ROLE TITLE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="15%"><Text text="TEAM" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="14%"><Text text="PERSONA" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="11%"><Text text="GRANTED DATE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="10%"><Text text="EXPIRY DATE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="10%"><Text text="STATUS" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="7%" hAlign="Center"><Text text="ACTION" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem vAlign="Middle">
                                <cells>
                                    <Text text="{accessModel>system}" class="fioriCellBold" />
                                    <Text text="{accessModel>roleName}" />
                                    <Text text="{= ${accessModel>team} || ${accessModel>serviceTopic} || ${accessModel>category} || 'Enterprise Security Team' }" />
                                    <Text text="{accessModel>persona}" />
                                    <Text text="{accessModel>grantedDate}" />
                                    <Text text="{= ${accessModel>expiryDate} === 'Permanent' ? '31 Dec 9999' : ${accessModel>expiryDate} }" />
                                    <ObjectStatus
                                        text="{accessModel>status}"
                                        state="{accessModel>statusState}" />
                                    <Button
                                        icon="sap-icon://overflow"
                                        type="Transparent"
                                        press=".onViewAccessDetails"
                                        tooltip="View entitlement &amp; audit summary"
                                        class="fioriThreeDotBtn" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>

                <!-- SECTION 2A: 5 HISTORY SUMMARY KPI BOXES (Standalone Row - No Outer White Card) -->
                <HBox width="100%" class="kyraHistoryKpiContainer sapUiSmallMarginBottom" wrap="Wrap" visible="{= !${accessModel>/showMyAccessMasterSection} &amp;&amp; ${accessModel>/selectedTabKey} === 'myRequests' }">
                    <!-- 1. ALL HISTORY BOX -->
                    <HBox id="kpiCardAll" class="kyraHistoryKpiCard kyraKpiAll fioriClickableCard" tooltip="Show All Requests">
                        <VBox class="kyraKpiContent">
                            <Text text="{= ${accessModel>/allHistoryCount} || (${accessModel>/requestHistory} ? ${accessModel>/requestHistory}.length : 0) }" class="kyraKpiCount" />
                            <Text text="All History" class="kyraKpiLabel" />
                        </VBox>
                        <core:Icon src="sap-icon://documents" class="kyraKpiIconBox" />
                    </HBox>

                    <!-- 2. PENDING BOX -->
                    <HBox id="kpiCardPending" class="kyraHistoryKpiCard kyraKpiPending fioriClickableCard" tooltip="Filter by Pending Requests">
                        <VBox class="kyraKpiContent">
                            <Text text="{= ${accessModel>/pendingHistoryCount} || 0 }" class="kyraKpiCount" />
                            <Text text="Pending" class="kyraKpiLabel" />
                        </VBox>
                        <core:Icon src="sap-icon://pending" class="kyraKpiIconBox" />
                    </HBox>

                    <!-- 3. EXPIRED BOX -->
                    <HBox id="kpiCardExpired" class="kyraHistoryKpiCard kyraKpiExpired fioriClickableCard" tooltip="Filter by Expired Requests">
                        <VBox class="kyraKpiContent">
                            <Text text="{= ${accessModel>/expiredHistoryCount} || 0 }" class="kyraKpiCount" />
                            <Text text="Expired" class="kyraKpiLabel" />
                        </VBox>
                        <core:Icon src="sap-icon://history" class="kyraKpiIconBox" />
                    </HBox>

                    <!-- 4. APPROVED BOX -->
                    <HBox id="kpiCardApproved" class="kyraHistoryKpiCard kyraKpiApproved fioriClickableCard" tooltip="Filter by Approved Requests">
                        <VBox class="kyraKpiContent">
                            <Text text="{= ${accessModel>/approvedHistoryCount} || 0 }" class="kyraKpiCount" />
                            <Text text="Approved" class="kyraKpiLabel" />
                        </VBox>
                        <core:Icon src="sap-icon://sys-enter-2" class="kyraKpiIconBox" />
                    </HBox>

                    <!-- 5. REJECTED BOX -->
                    <HBox id="kpiCardRejected" class="kyraHistoryKpiCard kyraKpiRejected fioriClickableCard" tooltip="Filter by Rejected Requests">
                        <VBox class="kyraKpiContent">
                            <Text text="{= ${accessModel>/rejectedHistoryCount} || 0 }" class="kyraKpiCount" />
                            <Text text="Rejected" class="kyraKpiLabel" />
                        </VBox>
                        <core:Icon src="sap-icon://error" class="kyraKpiIconBox" />
                    </HBox>
                </HBox>

                <!-- SECTION 2B: MY HISTORY TABLE (Visible on My History tab, can be closed via X button) -->
                <VBox id="historySectionContainer"
                    visible="{= !${accessModel>/showMyAccessMasterSection} &amp;&amp; ${accessModel>/selectedTabKey} === 'myRequests' &amp;&amp; ${accessModel>/showHistorySection} !== false }"
                    class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriSlideDownAnim">
                    
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                        <HBox alignItems="Center">
                            <Avatar src="{= ${accessModel>/historyFilterIcon} || 'sap-icon://documents' }" backgroundColor="{= ${accessModel>/historyFilterAvatarColor} || 'Accent6' }" displaySize="S" class="sapUiTinyMarginEnd fioriCardAvatar" />
                            <VBox>
                                <Title text="{= (${accessModel>/historyFilterTitle} || 'All History') + ' (' + (${accessModel>/filteredHistoryCount} !== undefined ? ${accessModel>/filteredHistoryCount} : (${accessModel>/requestHistory} ? ${accessModel>/requestHistory}.length : 0)) + ')' }" level="H4" class="fioriCardHeaderTitle" />
                                <Text text="{= ${accessModel>/historyFilterSubtitle} || 'All submitted and historical access requests.' }" class="fioriDescriptionText" />
                            </VBox>
                        </HBox>
                        <HBox alignItems="Center">
                            <SearchField
                                placeholder="Search request ID or role..."
                                width="220px"
                                liveChange=".onSearchRequests"
                                class="sapUiTinyMarginEnd" />
                            
                            <!-- FILTER BOX -->
                            <Button
                                icon="sap-icon://filter"
                                text="Filter"
                                type="Emphasized"
                                press=".onFilterHistoryDialog"
                                tooltip="Filter History by Status &amp; Duration"
                                class="sapUiTinyMarginEnd" />

                            <Button
                                icon="sap-icon://excel-attachment"
                                press=".onExportRequests"
                                tooltip="Export Audit Log"
                                class="sapUiTinyMarginEnd" />

                            <Button
                                icon="sap-icon://decline"
                                type="Transparent"
                                press=".onCloseHistorySection"
                                tooltip="Close Section" />
                        </HBox>
                    </HBox>

                    <!-- SINGLE CLEAN ALL-IN-ONE MY REQUESTS TABLE WITH PERSONA COLUMN -->
                    <Table
                        id="myRequestsUnifiedTable"
                        items="{
                            path: 'accessModel>/requestHistory'
                        }"
                        noDataText="No access requests match the selected filter."
                        class="sapUiNoMargin kyraHistoryTable">
                        <columns>
                            <Column width="12%"><Text text="REQUEST ID" class="kyraHistHeader" /></Column>
                            <Column width="7%"><Text text="TYPE" class="kyraHistHeader" /></Column>
                            <Column width="14%"><Text text="SYSTEMS" class="kyraHistHeader" /></Column>
                            <Column width="12%"><Text text="SERVICES" class="kyraHistHeader" /></Column>
                            <Column width="14%"><Text text="TEAM / ROLE" class="kyraHistHeader" /></Column>
                            <Column width="10%"><Text text="PERSONA" class="kyraHistHeader" /></Column>
                            <Column width="9%"><Text text="DURATION" class="kyraHistHeader" /></Column>
                            <Column width="8%"><Text text="SUBMITTED" class="kyraHistHeader" /></Column>
                            <Column width="14%" hAlign="Center"><Text text="STATUS" class="kyraHistHeader" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem vAlign="Middle" type="Active" press=".onOpenPendingRequestDetails" class="kyraClickableTableRow">
                                <cells>
                                    <Text text="{accessModel>requestId}" style="font-weight: 700; white-space: nowrap;" />
                                    <ObjectStatus
                                        text="{accessModel>type}"
                                        state="{= ${accessModel>type} === 'Revocation' ? 'Error' : 'None' }" />
                                    <Text text="{accessModel>system}" />
                                    <Text text="{accessModel>serviceTopic}" />
                                    <Text text="{accessModel>roleName}" />
                                    <Text text="{= ${accessModel>persona} || ${accessModel>selectedPersona} || 'Requester' }" style="white-space: nowrap;" />
                                    <Text text="{accessModel>accessDuration}" />
                                    <Text text="{accessModel>submissionDate}" style="white-space: nowrap;" />
                                    <ObjectStatus
                                        text="{accessModel>status}"
                                        state="{accessModel>statusState}"
                                        icon="{accessModel>statusIcon}"
                                        class="kyraApproverStatusBadge" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>

                <!-- DEDICATED SEPARATE PAGE VIEW: MY ACCESS MASTER DETAILS & AUDIT GOVERNANCE -->
                <VBox id="myAccessMasterSectionContainer" visible="{= ${accessModel>/showMyAccessMasterSection} }" class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriStepSlideAnim">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                        <HBox alignItems="Center">
                            <Avatar src="sap-icon://table-view" backgroundColor="Accent6" displaySize="S" class="sapUiSmallMarginEnd" />
                            <VBox>
                                <Title text="Active Entitlements — Complete Master View" level="H4" class="fioriCardHeaderTitle" />
                                <Text text="Comprehensive audit log &amp; active permission matrix for all enterprise roles." class="fioriDescriptionText" />
                            </VBox>
                        </HBox>
                        <HBox alignItems="Center">
                            <SearchField
                                placeholder="Search all entitlements..."
                                width="240px"
                                liveChange=".onSearchMasterAccess"
                                class="sapUiSmallMarginEnd" />
                            <Button icon="sap-icon://excel-attachment" text="Export Audit Log" type="Transparent" press=".onExportAccess" class="sapUiTinyMarginEnd fioriExportLinkBtn kyraTextBtn" />
                            <Button icon="sap-icon://decline" text="Close View" type="Transparent" press=".onCloseMyAccessMasterSection" class="fioriCloseLinkBtn kyraTextBtn" />
                        </HBox>
                    </HBox>

                    <Table
                        id="myAccessMasterSectionTable"
                        items="{accessModel>/userAccessList}"
                        noDataText="No active entitlements found."
                        class="sapUiNoMargin">
                        <columns>
                            <Column width="16%"><Text text="SYSTEM" class="fioriConflictTableHeader" /></Column>
                            <Column width="18%"><Text text="ROLE TITLE" class="fioriConflictTableHeader" /></Column>
                            <Column width="14%"><Text text="TEAM" class="fioriConflictTableHeader" /></Column>
                            <Column width="18%"><Text text="PERSONA" class="fioriConflictTableHeader" /></Column>
                            <Column width="10%"><Text text="GRANTED DATE" class="fioriConflictTableHeader" /></Column>
                            <Column width="9%"><Text text="EXPIRY DATE" class="fioriConflictTableHeader" /></Column>
                            <Column width="8%"><Text text="STATUS" class="fioriConflictTableHeader" /></Column>
                            <Column width="7%" hAlign="Center"><Text text="ACTION" class="fioriConflictTableHeader" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem vAlign="Middle">
                                <cells>
                                    <Text text="{accessModel>system}" class="fioriCellBold" />
                                    <Text text="{accessModel>roleName}" class="fioriCellBold" />
                                    <Text text="{= ${accessModel>team} || ${accessModel>serviceTopic} || ${accessModel>category} || 'Enterprise Security Team' }" />
                                    <Text text="{accessModel>persona}" />
                                    <Text text="{accessModel>grantedDate}" />
                                    <Text text="{= ${accessModel>expiryDate} === 'Permanent' ? '31 Dec 9999' : ${accessModel>expiryDate} }" />
                                    <ObjectStatus
                                        text="{accessModel>status}"
                                        state="{accessModel>statusState}" />
                                    <Button
                                        icon="sap-icon://hint"
                                        type="Transparent"
                                        press=".onViewAccessDetails"
                                        tooltip="View entitlement &amp; audit summary"
                                        class="fioriThreeDotBtn" />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>

                <!-- 3 FEATURE ACTION CARDS GRID - ONLY VISIBLE ON MY ACCESS TAB -->
                <HBox width="100%" class="fioriSingleRowCardsGrid" visible="{= !${accessModel>/showMyAccessMasterSection} &amp;&amp; (${accessModel>/selectedTabKey} || 'myAccess') === 'myAccess' }">
                    <!-- CARD 1: PENDING ACCESS REQUESTS -->
                    <VBox id="cardPendingRequests" class="fioriActionCard fioriClickableCard sapUiResponsivePadding" justifyContent="SpaceBetween">
                        <HBox alignItems="Start" class="fioriCardHeaderRow">
                            <Avatar src="sap-icon://history" backgroundColor="Accent3" displaySize="M" class="fioriCardAvatar fioriAmberAvatar" />
                            <VBox class="fioriCardTitleBox">
                                <Title text="Pending Access Requests" level="H5" class="fioriActionCardTitle" />
                                <Text text="Awaiting approval" class="fioriActionCardSub" />
                            </VBox>
                        </HBox>
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriCardFooterRow">
                            <Link text="{= ${accessModel>/myPendingRequests} ? (${accessModel>/myPendingRequests}.length &lt; 10 ? '0' + ${accessModel>/myPendingRequests}.length : '' + ${accessModel>/myPendingRequests}.length) : '00' }" press=".onNavToPendingRequests" class="fioriPendingNumber" />
                        </HBox>
                    </VBox>

                    <!-- CARD 2: ADD ACCESS -->
                    <VBox id="cardAddAccess" class="fioriActionCard fioriClickableCard sapUiResponsivePadding" justifyContent="SpaceBetween">
                        <HBox alignItems="Start" class="fioriCardHeaderRow">
                            <Avatar src="sap-icon://add" backgroundColor="Accent1" displaySize="M" class="fioriCardAvatar fioriBlueAvatar" />
                            <VBox class="fioriCardTitleBox">
                                <Title text="Add Access" level="H5" class="fioriActionCardTitle" />
                                <Text text="Request new access" class="fioriActionCardSub" />
                            </VBox>
                        </HBox>
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriCardFooterRow">
                            <Link text="Create Request" press=".onNavToAddAccess" class="fioriActionLink" />
                        </HBox>
                    </VBox>

                    <!-- CARD 3: REMOVE ACCESS -->
                    <VBox id="cardRemoveAccess" class="fioriActionCard fioriClickableCard sapUiResponsivePadding" justifyContent="SpaceBetween">
                        <HBox alignItems="Start" class="fioriCardHeaderRow">
                            <Avatar src="sap-icon://delete" backgroundColor="Accent2" displaySize="M" class="fioriCardAvatar fioriPurpleAvatar" />
                            <VBox class="fioriCardTitleBox">
                                <Title text="Remove Access" level="H5" class="fioriActionCardTitle" />
                                <Text text="Remove existing access" class="fioriActionCardSub" />
                            </VBox>
                        </HBox>
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriCardFooterRow">
                            <Link text="Remove Access" press=".onNavToRemoveAccess" class="fioriActionLink" />
                        </HBox>
                    </VBox>
                </HBox>

                <!-- IN-PAGE PENDING ACCESS REQUESTS TABLE (Toggled by Card 1 Click - Only on My Access Tab) -->
                <VBox id="pendingSectionContainer"
                    visible="{= (${accessModel>/selectedTabKey} || 'myAccess') === 'myAccess' &amp;&amp; !${accessModel>/showMyAccessMasterSection} &amp;&amp; !!${accessModel>/showPendingSection} }"
                    class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriSlideDownAnim">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                        <HBox alignItems="Center">
                            <Avatar src="sap-icon://history" backgroundColor="Accent3" displaySize="S" class="sapUiTinyMarginEnd fioriCardAvatar fioriAmberAvatar" />
                            <VBox>
                                <Title text="{= 'Pending Access Requests (' + (${accessModel>/myPendingRequests} ? ${accessModel>/myPendingRequests}.length : 0) + ')' }" level="H4" class="fioriCardHeaderTitle" />
                                <Text text="Requests currently awaiting manager or security approval." class="fioriDescriptionText" />
                            </VBox>
                        </HBox>
                        <Button icon="sap-icon://decline" type="Transparent" press=".onClosePendingSection" tooltip="Close Section" />
                    </HBox>

                    <Table
                        id="pendingRequestsInPageTable"
                        items="{accessModel>/myPendingRequests}"
                        noDataText="No pending access requests at this time."
                        class="sapUiNoMargin kyraHistoryTable">
                        <columns>
                            <Column width="13%"><Text text="REQUEST ID" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="8%"><Text text="TYPE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="14%"><Text text="SYSTEM" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="13%"><Text text="SERVICE / TOPIC" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="14%"><Text text="BUSINESS ROLE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="18%"><Text text="PERSONA" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="9%"><Text text="DURATION" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="11%"><Text text="STATUS" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem vAlign="Middle" type="Active" press=".onOpenPendingRequestDetails" class="kyraClickableTableRow">
                                <cells>
                                    <Text text="{accessModel>requestId}" style="font-weight: 700; white-space: nowrap;" />
                                    <ObjectStatus
                                        text="{accessModel>type}"
                                        state="{= ${accessModel>type} === 'Revocation' ? 'Error' : 'None' }" />
                                    <Text text="{accessModel>system}" />
                                    <Text text="{accessModel>serviceTopic}" />
                                    <Text text="{accessModel>roleName}" />
                                    <Text text="{= ${accessModel>persona} || ${accessModel>selectedPersona} || 'Frontend &amp; UI Developer Persona' }" />
                                    <Text text="{accessModel>accessDuration}" />
                                    <ObjectStatus
                                        text="Pending Approval"
                                        state="Warning"
                                        class="kyraApproverStatusBadge" />
                                 </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>

            <!-- DYNAMIC IN-PAGE ADD ACCESS WIZARD (Toggled by Card 3 Click - Same Page Dropdown Method) -->
            <VBox id="addAccessSectionContainer"
                    visible="{accessModel>/showAddAccessSector}"
                    class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriSlideDownAnim">
                    
                    <!-- Floating Header with Dynamic Title & 5-Step Progress Indicator -->
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriFloatingHeader">
                        <HBox alignItems="Center">
                            <Avatar src="{= ${accessModel>/addAccessStep} === 5 ? 'sap-icon://summary-detail' : (${accessModel>/addAccessStep} === 4 ? 'sap-icon://shield' : (${accessModel>/addAccessStep} === 3 ? 'sap-icon://user-settings' : (${accessModel>/addAccessStep} === 2 ? 'sap-icon://globe' : 'sap-icon://building'))) }"
                                backgroundColor="{= ${accessModel>/addAccessStep} === 5 ? 'Accent6' : (${accessModel>/addAccessStep} === 4 ? 'Accent8' : (${accessModel>/addAccessStep} === 3 ? 'Accent3' : (${accessModel>/addAccessStep} === 2 ? 'Accent2' : 'Accent1'))) }"
                                displaySize="S"
                                class="sapUiTinyMarginEnd fioriCardAvatar" />
                            <VBox>
                                <Title text="{= ${accessModel>/addAccessStep} === 5 ? 'Step 5: Summary' : (${accessModel>/addAccessStep} === 4 &amp;&amp; (${accessModel>/addAccessStep4SubStep} === 2 || ${accessModel>/addAccessStep4SubStep} === 3) ? 'Step 4: SoD Conflict Review' : (${accessModel>/addAccessStep} === 4 ? 'Step 4: Access Validation' : (${accessModel>/addAccessStep} === 3 ? 'Step 3: Config' : (${accessModel>/addAccessStep} === 2 ? 'Step 2: Region' : 'Step 1: Scope')))) }" level="H4" class="fioriCardHeaderTitle" />
                                <Text text="{= ${accessModel>/addAccessStep} === 5 ? 'Review configured target entitlements and submit request.' : (${accessModel>/addAccessStep} === 4 &amp;&amp; (${accessModel>/addAccessStep4SubStep} === 2 || ${accessModel>/addAccessStep4SubStep} === 3) ? 'Review and resolve Segregation of Duties (SoD) conflicts before proceeding.' : (${accessModel>/addAccessStep} === 4 ? 'Validate access threshold limits, restricted records, and duplicate roles.' : (${accessModel>/addAccessStep} === 3 ? 'Configure target systems, services, team roles, personas, duration, and justification.' : (${accessModel>/addAccessStep} === 2 ? 'Select target operating region.' : 'Select enterprise sector and operational function.')))) }" class="fioriDescriptionText" />
                            </VBox>
                        </HBox>
                        <Button
                            icon="sap-icon://decline"
                            type="Transparent"
                            press=".onCloseAddAccessSector"
                            tooltip="Close Add Access Section" />
                    </HBox>

                    <!-- ANIMATED 5-STEP PROGRESS TRACKER BAR -->
                    <HBox justifyContent="Center" alignItems="Center" class="sapUiSmallMarginBottom fioriWizardProgressTrack">
                        <!-- Step 1 Pill -->
                        <HBox alignItems="Center" class="{= ${accessModel>/addAccessStep} === 1 || !${accessModel>/addAccessStep} ? 'fioriStepPill fioriStepPillActive' : 'fioriStepPill fioriStepPillDone' }">
                            <Avatar src="{= ${accessModel>/addAccessStep} > 1 ? 'sap-icon://sys-enter-2' : 'sap-icon://building' }" displaySize="XS" class="sapUiTinyMarginEnd" />
                            <Text text="1. Scope" class="fioriStepPillText" />
                        </HBox>
                        
                        <HBox class="{= ${accessModel>/addAccessStep} > 1 ? 'fioriStepConnectorLine fioriStepConnectorActive' : 'fioriStepConnectorLine' }" />
                        
                        <!-- Step 2 Pill -->
                        <HBox alignItems="Center" class="{= ${accessModel>/addAccessStep} === 2 ? 'fioriStepPill fioriStepPillActive' : (${accessModel>/addAccessStep} > 2 ? 'fioriStepPill fioriStepPillDone' : 'fioriStepPill') }">
                            <Avatar src="{= ${accessModel>/addAccessStep} > 2 ? 'sap-icon://sys-enter-2' : 'sap-icon://globe' }" displaySize="XS" class="sapUiTinyMarginEnd" />
                            <Text text="2. Region" class="fioriStepPillText" />
                        </HBox>

                        <HBox class="{= ${accessModel>/addAccessStep} > 2 ? 'fioriStepConnectorLine fioriStepConnectorActive' : 'fioriStepConnectorLine' }" />

                        <!-- Step 3 Pill -->
                        <HBox alignItems="Center" class="{= ${accessModel>/addAccessStep} === 3 ? 'fioriStepPill fioriStepPillActive' : (${accessModel>/addAccessStep} > 3 ? 'fioriStepPill fioriStepPillDone' : 'fioriStepPill') }">
                            <Avatar src="{= ${accessModel>/addAccessStep} > 3 ? 'sap-icon://sys-enter-2' : 'sap-icon://user-settings' }" displaySize="XS" class="sapUiTinyMarginEnd" />
                            <Text text="3. Config" class="fioriStepPillText" />
                        </HBox>

                        <HBox class="{= ${accessModel>/addAccessStep} > 3 ? 'fioriStepConnectorLine fioriStepConnectorActive' : 'fioriStepConnectorLine' }" />

                        <!-- Step 4 Pill (Access Validation) -->
                        <HBox alignItems="Center" class="{= ${accessModel>/addAccessStep} === 4 ? 'fioriStepPill fioriStepPillActive' : (${accessModel>/addAccessStep} > 4 ? 'fioriStepPill fioriStepPillDone' : 'fioriStepPill') }">
                            <Avatar src="{= ${accessModel>/addAccessStep} > 4 ? 'sap-icon://sys-enter-2' : 'sap-icon://shield' }" displaySize="XS" class="sapUiTinyMarginEnd" />
                            <Text text="4. Access Validation" class="fioriStepPillText" />
                        </HBox>

                        <HBox class="{= ${accessModel>/addAccessStep} > 4 ? 'fioriStepConnectorLine fioriStepConnectorActive' : 'fioriStepConnectorLine' }" />

                        <!-- Step 5 Pill (Summary) -->
                        <HBox alignItems="Center" class="{= ${accessModel>/addAccessStep} === 5 ? 'fioriStepPill fioriStepPillActive' : 'fioriStepPill' }">
                            <Avatar src="sap-icon://summary-detail" displaySize="XS" class="sapUiTinyMarginEnd" />
                            <Text text="5. Summary" class="fioriStepPillText" />
                        </HBox>
                    </HBox>

                    <!-- STEP 1: BUSINESS SECTOR & FUNCTION -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 1 || !${accessModel>/addAccessStep} }" class="fioriStepSlideAnim">
                        <form:SimpleForm
                            editable="true"
                            layout="ColumnLayout"
                            columnsM="2"
                            columnsL="2"
                            singleContainerFullWidth="false"
                            class="fioriScopeGlassForm sapUiSmallMarginBottom">
                            <form:content>
                                <core:Title text="Enterprise Scope Selection" />
                                
                                <Label text="Business Sector" required="true" />
                                <ComboBox
                                    id="inPageBusinessSectorSelect"
                                    selectedKey="{accessModel>/selectedSector}"
                                    selectionChange=".onInPageSectorChange"
                                    placeholder="Select Business Sector..."
                                    width="100%"
                                    class="fioriFormSelect fioriSelectGlow">
                                    <core:Item key="Finance &amp; Enterprise Performance" text="Finance &amp; Enterprise Performance" />
                                    <core:Item key="Global Supply Chain &amp; Logistics" text="Global Supply Chain &amp; Logistics" />
                                    <core:Item key="Human Capital Management (HCM)" text="Human Capital Management (HCM)" />
                                    <core:Item key="Information Technology &amp; Security" text="Information Technology &amp; Security" />
                                    <core:Item key="Customer Operations &amp; Sales" text="Customer Operations &amp; Sales" />
                                </ComboBox>

                                <Label text="Business Function" required="true" />
                                <ComboBox
                                    id="inPageBusinessFunctionSelect"
                                    selectedKey="{accessModel>/selectedFunction}"
                                    items="{accessModel>/availableFunctions}"
                                    placeholder="Select Business Function..."
                                    width="100%"
                                    class="fioriFormSelect fioriSelectGlow">
                                    <core:Item key="{accessModel>key}" text="{accessModel>text}" />
                                </ComboBox>
                            </form:content>
                        </form:SimpleForm>

                        <HBox justifyContent="End" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Cancel"
                                type="Transparent"
                                press=".onCloseAddAccessSector"
                                class="kyraTextBtn sapUiSmallMarginEnd" />
                            <Button
                                text="Continue"
                                type="Emphasized"
                                icon="sap-icon://navigation-right-arrow"
                                iconFirst="false"
                                press=".onGoToAddAccessStep2"
                                class="kyraPrimaryBtn" />
                        </HBox>
                    </VBox>

                    <!-- STEP 2: REGION SELECTION (EXACT MATCH FOR USER UPLOADED IMAGE 2) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 2 }" class="fioriStepSlideAnim">
                        <!-- Card 1: World Map Container Box -->
                        <VBox class="fioriMapCardOuter sapUiSmallMarginBottom">
                            <core:HTML content='&lt;div id="mapWrapper" class="map-wrapper"&gt;
                                    &lt;img id="worldMapImgAccessPage" src="world-map.jpg" alt="World Map" class="world-map-img" draggable="false" onmousedown="return false;"&gt;
                                    &lt;div id="pinsLayer" class="map-pins-layer"&gt;&lt;/div&gt;
                                    
                                    &lt;div id="selectAllBtn" class="select-all-btn-wrapper"&gt;
                                      &lt;div class="select-all-icon"&gt;
                                        &lt;svg viewBox="0 0 24 24" width="12" height="12" fill="currentColor"&gt;
                                          &lt;path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5-2.5z"/&gt;
                                        &lt;/svg&gt;
                                      &lt;/div&gt;
                                      &lt;span class="select-all-text"&gt;Select All Regions&lt;/span&gt;
                                    &lt;/div&gt;
                                &lt;/div&gt;'>
                            </core:HTML>
                        </VBox>

                        <!-- Card 2: Selected Regions Box -->
                        <VBox class="selected-regions-card-box sapUiSmallMarginBottom" alignContent="Center" alignItems="Center">
                            <Text text="SELECTED REGIONS" class="selected-regions-header" />
                            <HBox id="selectedChipsList" items="{accessModel>/mapSelectedRegions}" class="selected-chips-list" justifyContent="Center" wrap="Wrap">
                                <Button 
                                    text="{accessModel>name}" 
                                    icon="sap-icon://decline" 
                                    iconFirst="false" 
                                    press=".onRemoveMapRegionChip"
                                    class="region-chip-btn sapUiTinyMarginEnd sapUiTinyMarginBottom" />
                            </HBox>
                            <Text text="No regions selected" class="no-selection-text" visible="{= !${accessModel>/hasMapRegionSelection} }" />
                        </VBox>

                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Previous"
                                icon="sap-icon://navigation-left-arrow"
                                iconFirst="true"
                                type="Default"
                                press=".onGoToAddAccessStep1"
                                class="kyraSecondaryBtn" />
                            <HBox alignItems="Center">
                                <Button
                                    text="Cancel"
                                    type="Transparent"
                                    press=".onCloseAddAccessSector"
                                    class="kyraTextBtn sapUiSmallMarginEnd" />
                                <Button
                                    text="Continue"
                                    type="Emphasized"
                                    icon="sap-icon://navigation-right-arrow"
                                    iconFirst="false"
                                    press=".onGoToAddAccessStep3"
                                    class="kyraPrimaryBtn" />
                            </HBox>
                        </HBox>
                    </VBox>

                    <!-- STEP 3: ACCESS CONFIGURATION (SEQUENTIAL TARGET SYSTEM SLIDES & DURATION/JUSTIFICATION) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 3 }" class="fioriStepSlideAnim">
                        
                        <!-- SUB-STEP 3.1: TARGET SYSTEMS SLIDE CONFIGURATION -->
                        <VBox visible="{= ${accessModel>/addAccessConfigSubStep} === 1 || !${accessModel>/addAccessConfigSubStep} }">
                            <form:SimpleForm
                                editable="true"
                                layout="ColumnLayout"
                                columnsM="2"
                                columnsL="2"
                                singleContainerFullWidth="false"
                                class="sapUiSmallMarginBottom">
                                <form:content>
                                    <core:Title text="Target Systems &amp; Process Selection" />
                                    
                                    <Label text="Select Target Systems &amp; Processes" required="true" />
                                    <MultiComboBox
                                        id="inPageSystemsMultiSelect"
                                        selectedKeys="{accessModel>/addAccessSelectedSystems}"
                                        selectionChange=".onInPageTargetSystemsSelectionChange"
                                        placeholder="Select target system processes..."
                                        width="100%"
                                        class="fioriFormSelect">
                                        <core:Item key="SAP BTP Cloud Platform" text="SAP BTP Cloud Platform" icon="sap-icon://cloud" />
                                        <core:Item key="SAP S/4HANA Enterprise" text="SAP S/4HANA Enterprise" icon="sap-icon://database" />
                                        <core:Item key="KYRA Central Governance" text="KYRA Central Governance" icon="sap-icon://shield" />
                                        <core:Item key="Active Directory / IAM" text="Active Directory / IAM" icon="sap-icon://user-settings" />
                                        <core:Item key="SAP SuccessFactors" text="SAP SuccessFactors" icon="sap-icon://group" />
                                        <core:Item key="SAP Ariba Supply Network" text="SAP Ariba Supply Network" icon="sap-icon://shipping-status" />
                                    </MultiComboBox>
                                </form:content>
                            </form:SimpleForm>

                            <!-- PER-TARGET SYSTEM SLIDE CARD -->
                            <VBox visible="{= ${accessModel>/addAccessSelectedSystems} &amp;&amp; ${accessModel>/addAccessSelectedSystems}.length &gt; 0 }" class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom">
                                <!-- Target System Slide Header Banner -->
                                <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom fioriConflictHeaderBanner">
                                    <HBox alignItems="Center">
                                        <Avatar src="sap-icon://action-settings" backgroundColor="Accent6" displaySize="S" class="sapUiTinyMarginEnd" />
                                        <VBox>
                                            <Title text="{= 'Target System Slide (' + (${accessModel>/addAccessCurrentSystemIndex} + 1) + ' of ' + ${accessModel>/addAccessSelectedSystems}.length + '): ' + ${accessModel>/currentSystemSlideName} }" level="H4" class="fioriCardHeaderTitle" />
                                            <Text text="Configure specific services, team roles, and personas for this target system." class="fioriDescriptionText" />
                                        </VBox>
                                    </HBox>
                                    <ObjectStatus
                                        text="{= 'Slide ' + (${accessModel>/addAccessCurrentSystemIndex} + 1) + ' / ' + ${accessModel>/addAccessSelectedSystems}.length }"
                                        state="Information" />
                                </HBox>

                                <form:SimpleForm
                                    editable="true"
                                    layout="ColumnLayout"
                                    columnsM="2"
                                    columnsL="2"
                                    singleContainerFullWidth="false">
                                    <form:content>
                                        <core:Title text="{= 'Entitlements &amp; Personas for ' + ${accessModel>/currentSystemSlideName} }" />
                                        
                                        <!-- FIELD 1: SERVICE / TOPIC -->
                                        <Label text="Service / Topic" required="true" />
                                        <MultiComboBox
                                            id="inPageServicesMultiSelect"
                                            selectedKeys="{accessModel>/addAccessSelectedServices}"
                                            selectionChange=".onInPageServicesSelectionChange"
                                            placeholder="Select service topics for this system..."
                                            width="100%"
                                            class="fioriFormSelect">
                                            <core:Item key="System Administrator" text="System Administrator" />
                                            <core:Item key="System Owners" text="System Owners" />
                                            <core:Item key="Stakeholders" text="Stakeholders" />
                                        </MultiComboBox>

                                        <!-- FIELD 2: TEAM ROLE -->
                                        <Label text="Team Role" required="true" />
                                        <MultiComboBox
                                            id="inPageTeamMultiSelect"
                                            selectedKeys="{accessModel>/addAccessSelectedRoles}"
                                            selectionChange=".onInPageTeamSelectionChange"
                                            items="{accessModel>/addAccessSubRolesList}"
                                            placeholder="{= ${accessModel>/addAccessSelectedServices} &amp;&amp; ${accessModel>/addAccessSelectedServices}.length &gt; 0 ? 'Select team roles for this system...' : 'Select Service / Topic above first...' }"
                                            width="100%"
                                            enabled="{= ${accessModel>/addAccessSelectedServices} &amp;&amp; ${accessModel>/addAccessSelectedServices}.length &gt; 0 }"
                                            class="fioriFormSelect">
                                            <core:Item key="{accessModel>key}" text="{accessModel>text}" icon="{accessModel>icon}" />
                                        </MultiComboBox>

                                        <!-- FIELD 3: ASSIGNED PERSONA -->
                                        <Label text="Assigned Persona" required="true" />
                                        <MultiComboBox
                                            id="inPagePersonaMultiSelect"
                                            selectedKeys="{accessModel>/addAccessSelectedPersonas}"
                                            items="{accessModel>/addAccessPersonasList}"
                                            placeholder="{= ${accessModel>/addAccessSelectedRoles} &amp;&amp; ${accessModel>/addAccessSelectedRoles}.length &gt; 0 ? 'Select personas for this system...' : 'Select Team Role above first...' }"
                                            width="100%"
                                            enabled="{= ${accessModel>/addAccessSelectedRoles} &amp;&amp; ${accessModel>/addAccessSelectedRoles}.length &gt; 0 }"
                                            class="fioriFormSelect">
                                            <core:Item key="{accessModel>key}" text="{accessModel>text}" icon="{accessModel>icon}" />
                                        </MultiComboBox>
                                    </form:content>
                                </form:SimpleForm>
                            </VBox>

                            <!-- Single unified footer action bar -->
                            <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                                <Button
                                    text="Previous"
                                    icon="sap-icon://navigation-left-arrow"
                                    iconFirst="true"
                                    type="Default"
                                    press=".onStep3Slide1Previous"
                                    class="kyraSecondaryBtn" />
                                <HBox alignItems="Center">
                                    <Button
                                        text="Cancel"
                                        type="Transparent"
                                        press=".onCloseAddAccessSector"
                                        class="kyraTextBtn sapUiSmallMarginEnd" />
                                    <Button
                                        text="Continue"
                                        type="Emphasized"
                                        icon="sap-icon://navigation-right-arrow"
                                        iconFirst="false"
                                        press=".onStep3Slide1Continue"
                                        class="kyraPrimaryBtn" />
                                </HBox>
                            </HBox>
                        </VBox>

                        <!-- SUB-STEP 3.2: DURATION & BUSINESS JUSTIFICATION -->
                        <VBox visible="{= ${accessModel>/addAccessConfigSubStep} === 2 }">
                            <form:SimpleForm
                                editable="true"
                                layout="ColumnLayout"
                                columnsM="2"
                                columnsL="2"
                                singleContainerFullWidth="false"
                                class="sapUiSmallMarginBottom">
                                <form:content>
                                    <core:Title text="Access Duration &amp; Business Justification" />
                                    
                                    <!-- SUB-STEP 3.2 - FIELD 1: DURATION -->
                                    <Label text="Access Duration" required="true" />
                                    <ComboBox
                                        id="inPageDurationSelect"
                                        selectedKey="{accessModel>/addAccessDuration}"
                                        placeholder="Select Access Duration..."
                                        width="100%"
                                        class="fioriFormSelect">
                                        <core:Item key="Permanent (Default)" text="Permanent (Default)" />
                                        <core:Item key="30 Days (Temporary)" text="30 Days (Temporary)" />
                                        <core:Item key="90 Days (Project)" text="90 Days (Project)" />
                                    </ComboBox>

                                    <!-- SUB-STEP 3.2 - FIELD 2: JUSTIFICATION -->
                                    <Label text="Business Justification" required="true" />
                                    <TextArea
                                        value="{accessModel>/addAccessJustification}"
                                        placeholder="Provide detailed business justification for requesting these system permissions..."
                                        rows="4"
                                        width="100%" />
                                </form:content>
                            </form:SimpleForm>

                            <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                                <Button
                                    text="Previous"
                                    icon="sap-icon://navigation-left-arrow"
                                    iconFirst="true"
                                    type="Default"
                                    visible="{= !${accessModel>/isEditingFromSummary} }"
                                    press=".onBackToSystemSlides"
                                    class="kyraSecondaryBtn" />
                                <Text text="" visible="{accessModel>/isEditingFromSummary}" />
                                <HBox alignItems="Center">
                                    <!-- Simple OK button shown ONLY when editing from Summary -->
                                    <Button
                                        text="OK"
                                        type="Emphasized"
                                        icon="sap-icon://accept"
                                        visible="{accessModel>/isEditingFromSummary}"
                                        press=".onSaveAndReturnToSummary"
                                        class="kyraPrimaryBtn"
                                        tooltip="Save changes and return straight to Summary" />
                                    <Button
                                        text="Cancel"
                                        type="Transparent"
                                        visible="{= !${accessModel>/isEditingFromSummary} }"
                                        press=".onCloseAddAccessSector"
                                        class="kyraTextBtn sapUiSmallMarginEnd" />
                                    <Button
                                        text="Continue"
                                        type="Emphasized"
                                        icon="sap-icon://navigation-right-arrow"
                                        iconFirst="false"
                                        visible="{= !${accessModel>/isEditingFromSummary} }"
                                        press=".onGoToAddAccessStep4"
                                        class="kyraPrimaryBtn" />
                                </HBox>
                            </HBox>
                        </VBox>
                    </VBox>

                    <!-- STEP 4: ACCESS VALIDATION & SOD CONFLICT REVIEW (2-SLIDE PROCESS) -->

                    <!-- SLIDE 1: ACCESS VALIDATION (THRESHOLD LIMITS, RESTRICTED RECORDS, DUPLICATE ROLES) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 4 &amp;&amp; (${accessModel>/addAccessStep4SubStep} || 1) === 1 }" class="fioriStepSlideAnim sapUiResponsivePadding">
                        <!-- Sub Header Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriConflictHeaderBanner sapUiSmallMarginBottom">
                            <HBox alignItems="Center">
                                <Link text="&lt; Back to Duration &amp; Justification" press=".onGoBackToDurationSlide" class="fioriConflictBackLink sapUiSmallMarginEnd" />
                                <Title text="Step 4: Access Validation" level="H4" class="fioriBannerTitle sapUiSmallMarginBegin" />
                            </HBox>
                            <HBox alignItems="Center">
                                <Text text="Requester ID: " class="fioriRequestForLabel" />
                                <Text text="{= ${accessModel>/activeUser} || 'Dev001' }" class="fioriRequestForUser sapUiTinyMarginBegin" />
                                <Button icon="sap-icon://home" type="Transparent" press=".onNavBackToPortal" tooltip="Back to Portal" class="sapUiTinyMarginBegin" />
                            </HBox>
                        </HBox>

                        <!-- SECTION 1: Threshold Limits Card -->
                        <VBox class="kyraValidationCard sapUiSmallMarginBottom">
                            <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                                <HBox alignItems="Center">
                                    <Avatar src="sap-icon://performance" class="kyraValAvatarBlue sapUiSmallMarginEnd" displaySize="S" />
                                    <VBox>
                                        <Title text="Threshold Limits" level="H5" class="kyraValCardTitle" />
                                        <Text text="Validate access against defined threshold limits." class="kyraValCardSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) ? 'kyraPillBadgeError' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) ? (${accessModel>/thresholdLimits}.length + ' Issues Exceeded') : '0 Issues' }" class="{= (${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) ? 'kyraPillBadgeErrorText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>
                            
                            <Table id="thresholdLimitsTable" items="{accessModel>/thresholdLimits}" visible="{= !!(${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) }" class="kyraValTable" fixedLayout="false" width="100%">
                                <columns>
                                    <Column width="25%"><Text text="SYSTEM" class="kyraValColHeader" /></Column>
                                    <Column width="25%"><Text text="SECTOR" class="kyraValColHeader" /></Column>
                                    <Column width="25%"><Text text="LIMIT" class="kyraValColHeader" /></Column>
                                    <Column width="25%"><Text text="EXCESSIVE PERCENTAGE" class="kyraValColHeader" /></Column>
                                </columns>
                                <items>
                                    <ColumnListItem vAlign="Middle" class="kyraValTableRow">
                                        <cells>
                                            <Text text="{accessModel>system}" class="kyraValBoldText kyraValCellWrap" />
                                            <Text text="{accessModel>sector}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>limit}" class="kyraValNormalText kyraValCellWrap" />
                                            <ObjectStatus text="{accessModel>excessivePercentage}" state="Error" icon="sap-icon://alert" />
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                            <VBox class="kyraValEmptyStateBox" visible="{= !(${accessModel>/thresholdLimits} &amp;&amp; ${accessModel>/thresholdLimits}.length > 0) }" alignItems="Center" justifyContent="Center">
                                <HBox class="kyraValClipboardBadge" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://survey" class="kyraValClipboardIcon" />
                                    <core:Icon src="sap-icon://sys-enter-2" class="kyraValCheckmarkIcon" />
                                </HBox>
                                <Text text="No Threshold Limits Exceeded" class="kyraValEmptyTitle" />
                                <Text text="All access levels are within the defined threshold limits." class="kyraValEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- SECTION 2: Restricted Records Card -->
                        <VBox class="kyraValidationCard sapUiSmallMarginBottom">
                            <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                                <HBox alignItems="Center">
                                    <Avatar src="sap-icon://role" class="kyraValAvatarPurple sapUiSmallMarginEnd" displaySize="S" />
                                    <VBox>
                                        <Title text="Restricted Records" level="H5" class="kyraValCardTitle" />
                                        <Text text="List of access records with restricted permissions." class="kyraValCardSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length > 0) ? 'kyraPillBadgeWarning' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length ? ${accessModel>/restrictedRecords}.length : 0) + ' Records' }" class="{= (${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length > 0) ? 'kyraPillBadgeWarningText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>
                            
                            <Table id="restrictedRecordsTable" items="{accessModel>/restrictedRecords}" visible="{= !!(${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length > 0) }" class="kyraValTable" fixedLayout="false" width="100%">
                                <columns>
                                    <!-- 1. System -->
                                    <Column width="14%" hAlign="Begin">
                                        <Text text="System" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 2. Role Title -->
                                    <Column width="15%" hAlign="Begin">
                                        <Text text="Role Title" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 3. Business Sector -->
                                    <Column width="15%" hAlign="Begin">
                                        <Text text="Business Sector" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 4. Business Function -->
                                    <Column width="14%" hAlign="Begin">
                                        <Text text="Business Function" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 5. Persona -->
                                    <Column width="18%" hAlign="Begin">
                                        <Text text="Persona" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 6. Security Group -->
                                    <Column width="12%" hAlign="Begin">
                                        <Text text="Security Group" class="kyraValColHeader" />
                                    </Column>
                                    <!-- 7. AD Group -->
                                    <Column width="12%" hAlign="Begin">
                                        <Text text="AD Group" class="kyraValColHeader" />
                                    </Column>
                                </columns>
                                <items>
                                    <ColumnListItem vAlign="Middle" class="kyraValTableRow">
                                        <cells>
                                            <Text text="{accessModel>system}" class="kyraValBoldText kyraValCellWrap" />
                                            <Text text="{accessModel>roleTitle}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>sector}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>businessFunction}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>persona}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>securityGroup}" class="kyraValMonospaceBadge kyraValCellWrap" />
                                            <Text text="{accessModel>adGroupName}" class="kyraValMonospaceBadge kyraValCellWrap" />
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                            <VBox class="kyraValEmptyStateBox" visible="{= !(${accessModel>/restrictedRecords} &amp;&amp; ${accessModel>/restrictedRecords}.length > 0) }" alignItems="Center" justifyContent="Center">
                                <HBox class="kyraValClipboardBadge" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://survey" class="kyraValClipboardIcon" />
                                    <core:Icon src="sap-icon://sys-enter-2" class="kyraValCheckmarkIcon" />
                                </HBox>
                                <Text text="No Restricted Records Found" class="kyraValEmptyTitle" />
                                <Text text="All requested records have standard non-restricted permissions." class="kyraValEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- SECTION 3: Duplicate Roles Card -->
                        <VBox class="kyraValidationCard sapUiSmallMarginBottom">
                            <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                                <HBox alignItems="Center">
                                    <Avatar src="sap-icon://copy" class="kyraValAvatarTeal sapUiSmallMarginEnd" displaySize="S" />
                                    <VBox>
                                        <Title text="Duplicate Roles" level="H5" class="kyraValCardTitle" />
                                        <Text text="Detect duplicate functional roles and assignments." class="kyraValCardSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) ? 'kyraPillBadgeWarning' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) ? (${accessModel>/duplicateRoles}.length + ' Duplicates Detected') : '0 Duplicates' }" class="{= (${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) ? 'kyraPillBadgeWarningText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>
                            
                            <Table id="duplicateRolesTable" items="{accessModel>/duplicateRoles}" visible="{= !!(${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) }" class="kyraValTable" fixedLayout="false" width="100%">
                                <columns>
                                    <Column width="14%"><Text text="SYSTEM" class="kyraValColHeader" /></Column>
                                    <Column width="15%"><Text text="FUNCTIONAL ROLE" class="kyraValColHeader" /></Column>
                                    <Column width="14%"><Text text="MODULE NAME" class="kyraValColHeader" /></Column>
                                    <Column width="17%"><Text text="SELECTED SECURITY GROUPS" class="kyraValColHeader" /></Column>
                                    <Column width="13%"><Text text="TEAM NAME" class="kyraValColHeader" /></Column>
                                    <Column width="13%"><Text text="AD GROUP NAME" class="kyraValColHeader" /></Column>
                                    <Column width="14%"><Text text="EXISTING GROUP/ ROLES" class="kyraValColHeader" /></Column>
                                </columns>
                                <items>
                                    <ColumnListItem vAlign="Middle" class="kyraValTableRow">
                                        <cells>
                                            <Text text="{accessModel>system}" class="kyraValBoldText kyraValCellWrap" />
                                            <Text text="{accessModel>functionalRole}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>moduleName}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>selectedSecurityGroups}" class="kyraValMonospaceBadge kyraValCellWrap" />
                                            <Text text="{accessModel>teamName}" class="kyraValNormalText kyraValCellWrap" />
                                            <Text text="{accessModel>adGroupName}" class="kyraValMonospaceBadge kyraValCellWrap" />
                                            <ObjectStatus text="{accessModel>existingRoles}" state="Warning" icon="sap-icon://alert" />
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>
                            <VBox class="kyraValEmptyStateBox" visible="{= !(${accessModel>/duplicateRoles} &amp;&amp; ${accessModel>/duplicateRoles}.length > 0) }" alignItems="Center" justifyContent="Center">
                                <HBox class="kyraValClipboardBadgeTeal" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://survey" class="kyraValClipboardIconTeal" />
                                    <core:Icon src="sap-icon://sys-enter-2" class="kyraValCheckmarkIconTeal" />
                                </HBox>
                                <Text text="No Duplicate Roles Detected" class="kyraValEmptyTitle" />
                                <Text text="No duplicate functional roles found across selected security groups." class="kyraValEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- Slide 1 Footer Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Previous"
                                icon="sap-icon://navigation-left-arrow"
                                iconFirst="true"
                                type="Default"
                                press=".onGoBackToDurationSlide"
                                class="kyraSecondaryBtn" />
                            <HBox alignItems="Center">
                                <Button text="Cancel" type="Transparent" press=".onCloseAddAccessSector" class="kyraTextBtn sapUiSmallMarginEnd" />
                                <Button text="Continue" type="Emphasized" icon="sap-icon://navigation-right-arrow" iconFirst="false" press=".onGoToStep4Slide2" class="kyraPrimaryBtn" />
                            </HBox>
                        </HBox>
                    </VBox>

                    <!-- SLIDE 2: SOD CONFLICT REVIEW (COMPACT VIEWPORT FIT) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 4 &amp;&amp; ${accessModel>/addAccessStep4SubStep} === 2 }" class="fioriStepSlideAnim kyraSodSlideContainer">
                        <!-- Top Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="kyraSodActionBar" width="100%">
                            <HBox alignItems="Center">
                                <Link text="&lt; Back to Access Validation" press=".onGoToStep4Slide1" class="kyraSodBackLink sapUiSmallMarginEnd" />
                                <Text text="|" class="kyraSodDivider sapUiSmallMarginEnd" />
                                <Text text="Requester ID:" class="kyraSodReqLabel sapUiTinyMarginEnd" />
                                <Text text="{= ${accessModel>/activeUser} || 'Dev001' }" class="kyraSodReqValue" />
                            </HBox>
                            <HBox alignItems="Center">
                                <Button text="Existing Access" icon="sap-icon://shield" type="Default" press=".onGoToStep4Slide2" class="kyraSodTopBtn kyraSodTopBtnActive sapUiSmallMarginEnd" />
                                <Button text="New Access" icon="sap-icon://add" type="Default" press=".onGoToStep4Slide3" class="kyraSodTopBtn kyraSodTopBtnInactive sapUiSmallMarginEnd" />
                                <Button text="Need help?" icon="sap-icon://sys-help" type="Transparent" class="kyraSodHelpBtn" />
                            </HBox>
                        </HBox>

                        <!-- SECTION 1: Conflicts with Active / Existing Access (Light Red Card with Red Conflict Indicators) -->
                        <VBox class="kyraSodRedCard">
                            <!-- Card Header -->
                            <HBox justifyContent="SpaceBetween" alignItems="Center" width="100%">
                                <HBox alignItems="Center">
                                    <Avatar src="sap-icon://alert" class="kyraSodAvatarRed sapUiSmallMarginEnd" displaySize="S" />
                                    <VBox>
                                        <Title text="Conflicts with Active / Existing Access" level="H5" class="kyraSodTitleRed" />
                                        <Text text="Conflicts detected against your active system access entitlements on the same system." class="kyraSodSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length > 0) ? 'kyraPillBadgeDanger' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length ? ${accessModel>/activeSodConflictsList}.length : 0) + ' Conflicts' }" class="{= (${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length > 0) ? 'kyraPillBadgeDangerText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>

                            <!-- 3-Column Header Bar: 35% / 35% / 30% -->
                            <HBox class="kyraSodTableHeadRed" width="100%" alignItems="Center" visible="{= ${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length > 0 }">
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="EXISTING ACCESS" class="kyraSodColHeader" /></HBox>
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="NEWLY ADDED ACCESS" class="kyraSodColHeader" /></HBox>
                                <HBox width="30%" class="kyraSodHeadCol"><Text text="SEGREGATION OF DUTIES (SOD) CONFLICT DETAIL" class="kyraSodColHeader" /></HBox>
                            </HBox>

                            <!-- Dynamic Conflict Rows when active conflicts exist -->
                            <VBox items="{accessModel>/activeSodConflictsList}" visible="{= ${accessModel>/activeSodConflictsList} &amp;&amp; ${accessModel>/activeSodConflictsList}.length > 0 }">
                                <HBox class="kyraSodContentRow" alignItems="Center" width="100%">
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxRedCard1" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://customer" class="kyraSodAvatarRed sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>existingRole}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>existingPersona}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxRedCard2" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://database" class="kyraSodAvatarBlue sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>newRole}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>newPersona}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                    <HBox width="30%" class="kyraSodColWrapper kyraSodConflictWrapper" alignItems="Center">
                                        <HBox class="kyraSodConflictDetailBoxRed" width="100%" alignItems="Center">
                                            <core:Icon src="sap-icon://alert" class="kyraSodAlertLargeIconRed sapUiTinyMarginEnd" />
                                            <VBox class="kyraSodConflictTextGroup">
                                                <Text text="{accessModel>conflictTitle}" class="kyraSodConflictMainTitleRed" />
                                                <Text text="{accessModel>conflictDesc}" class="kyraSodConflictMainDesc" />
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                </HBox>
                            </VBox>

                            <!-- Compact Empty State when NO active conflicts exist -->
                            <VBox class="kyraSodEmptyBoxRed" alignItems="Center" justifyContent="Center" visible="{= !${accessModel>/activeSodConflictsList} || ${accessModel>/activeSodConflictsList}.length === 0 }">
                                <HBox class="kyraSodRedCircleCompact" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://accept" class="kyraSodMintIconCompact" />
                                </HBox>
                                <Text text="No SoD Conflicts with Active / Existing Access" class="kyraSodEmptyTitleGreen" />
                                <Text text="Great! No conflicts found between your active access and new requests." class="kyraSodEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- SECTION 2: Conflicts with In-Flight / Pending Access Requests (Light Yellow Card with Yellow Warning Indicators) -->
                        <VBox class="kyraSodYellowCard">
                            <!-- Card Header -->
                            <HBox justifyContent="SpaceBetween" alignItems="Center" width="100%">
                                <HBox alignItems="Center">
                                    <Avatar src="sap-icon://alert" class="kyraSodAvatarAmber sapUiSmallMarginEnd" displaySize="S" />
                                    <VBox>
                                        <Title text="Conflicts with In-Flight / Pending Access Requests" level="H5" class="kyraSodTitleAmber" />
                                        <Text text="Conflicts detected against your pending access requests on the same system." class="kyraSodSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length > 0) ? 'kyraPillBadgeWarning' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length ? ${accessModel>/pendingOnlySodConflictsList}.length : 0) + ' Conflicts' }" class="{= (${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length > 0) ? 'kyraPillBadgeWarningText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>

                            <!-- 3-Column Header Bar: 35% / 35% / 30% -->
                            <HBox class="kyraSodTableHeadYellow" width="100%" alignItems="Center" visible="{= ${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length > 0 }">
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="PENDING ACCESS REQUEST" class="kyraSodColHeader" /></HBox>
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="NEWLY ADDED ACCESS" class="kyraSodColHeader" /></HBox>
                                <HBox width="30%" class="kyraSodHeadCol"><Text text="SEGREGATION OF DUTIES (SOD) CONFLICT DETAIL" class="kyraSodColHeader" /></HBox>
                            </HBox>

                            <!-- Dynamic Content Rows when pending conflicts exist -->
                            <VBox items="{accessModel>/pendingOnlySodConflictsList}" visible="{= ${accessModel>/pendingOnlySodConflictsList} &amp;&amp; ${accessModel>/pendingOnlySodConflictsList}.length > 0 }">
                                <HBox class="kyraSodContentRow" alignItems="Center" width="100%">
                                    <!-- Column 1: Existing & Pending Access (35%) -->
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxYellowCard1" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://customer" class="kyraSodAvatarYellow sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>existingRole}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>existingPersona}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>

                                    <!-- Column 2: Newly Added Access (35%) -->
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxYellowCard2" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://database" class="kyraSodAvatarBlue sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>newRole}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>newPersona}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>

                                    <!-- Column 3: Segregation of Duties Conflict Detail (30%) - Vertically Centered -->
                                    <HBox width="30%" class="kyraSodColWrapper kyraSodConflictWrapper" alignItems="Center">
                                        <HBox class="kyraSodConflictDetailBoxAmber" width="100%" alignItems="Center">
                                            <core:Icon src="sap-icon://alert" class="kyraSodAlertLargeIconAmber sapUiTinyMarginEnd" />
                                            <VBox class="kyraSodConflictTextGroup">
                                                <Text text="{accessModel>conflictTitle}" class="kyraSodConflictMainTitleAmber" />
                                                <Text text="{accessModel>conflictDesc}" class="kyraSodConflictMainDesc" />
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                </HBox>
                            </VBox>

                            <!-- Clean Empty State when NO pending conflicts exist -->
                            <VBox class="kyraSodEmptyBoxRed" alignItems="Center" justifyContent="Center" visible="{= !${accessModel>/pendingOnlySodConflictsList} || ${accessModel>/pendingOnlySodConflictsList}.length === 0 }">
                                <HBox class="kyraSodRedCircleCompact" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://accept" class="kyraSodMintIconCompact" />
                                </HBox>
                                <Text text="No SoD Conflicts with In-Flight / Pending Access Requests" class="kyraSodEmptyTitleGreen" />
                                <Text text="Great! No conflicts detected against your pending access requests." class="kyraSodEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- Slide 2 Footer Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Previous"
                                icon="sap-icon://navigation-left-arrow"
                                iconFirst="true"
                                type="Default"
                                press=".onGoToStep4Slide1"
                                class="kyraSecondaryBtn" />
                            <HBox alignItems="Center">
                                <Button text="Cancel" type="Transparent" press=".onCloseAddAccessSector" class="kyraTextBtn sapUiSmallMarginEnd" />
                                <Button
                                    text="Continue"
                                    type="Emphasized"
                                    icon="sap-icon://navigation-right-arrow"
                                    iconFirst="false"
                                    press=".onGoToStep4Slide3"
                                    class="kyraPrimaryBtn" />
                            </HBox>
                        </HBox>
                    </VBox>

                    <!-- SLIDE 3: CURRENT REQUEST BATCH (INTRA-ROLE) SOD CONFLICT (MATCHING USER SCREENSHOT) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 4 &amp;&amp; ${accessModel>/addAccessStep4SubStep} === 3 }" class="fioriStepSlideAnim kyraSodSlideContainer">
                        <!-- Top Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="kyraSodActionBar" width="100%">
                            <HBox alignItems="Center">
                                <Link text="&lt; Back to Access Validation" press=".onGoToStep4Slide1" class="kyraSodBackLink sapUiSmallMarginEnd" />
                                <Text text="|" class="kyraSodDivider sapUiSmallMarginEnd" />
                                <Text text="Requester ID:" class="kyraSodReqLabel sapUiTinyMarginEnd" />
                                <Text text="{= ${accessModel>/activeUser} || 'Dev001' }" class="kyraSodReqValue" />
                            </HBox>
                            <HBox alignItems="Center">
                                <Button text="Existing Access" icon="sap-icon://shield" type="Default" press=".onGoToStep4Slide2" class="kyraSodTopBtn kyraSodTopBtnInactive sapUiSmallMarginEnd" />
                                <Button text="New Access" icon="sap-icon://add" type="Default" press=".onGoToStep4Slide3" class="kyraSodTopBtn kyraSodTopBtnActive sapUiSmallMarginEnd" />
                                <Button text="Need help?" icon="sap-icon://sys-help" type="Transparent" class="kyraSodHelpBtn" />
                            </HBox>
                        </HBox>

                        <!-- Main Status Card for Batch Intra-Role Conflicts -->
                        <VBox class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraSodRedCard' : 'kyraSodGreenCard' }">
                            <!-- Card Header -->
                            <HBox justifyContent="SpaceBetween" alignItems="Center" width="100%">
                                <HBox alignItems="Center">
                                    <Avatar src="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'sap-icon://alert' : 'sap-icon://document-text' }" class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraSodAvatarRed sapUiSmallMarginEnd' : 'kyraSodAvatarGreen sapUiSmallMarginEnd' }" displaySize="S" />
                                    <VBox>
                                        <Title text="Conflicts Within Current Request Batch (Intra-Role)" level="H5" class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraSodTitleRed' : 'kyraSodTitleGreen' }" />
                                        <Text text="Conflicts detected between multiple roles selected together in this request on the same system." class="kyraSodSubtitle" />
                                    </VBox>
                                </HBox>
                                <HBox class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraPillBadgeDanger' : 'kyraPillBadgeSuccess' }" alignItems="Center">
                                    <core:Icon src="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'sap-icon://alert' : 'sap-icon://accept' }" size="12px" class="sapUiTinyMarginEnd" />
                                    <Text text="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length ? ${accessModel>/batchSodConflictsList}.length : 0) + ' Conflicts' }" class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraPillBadgeDangerText' : 'kyraPillBadgeSuccessText' }" />
                                </HBox>
                            </HBox>

                            <!-- 3-Column Header Bar: 35% / 35% / 30% -->
                            <HBox class="{= (${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0) ? 'kyraSodTableHeadRed' : 'kyraSodTableHeadGreen' }" width="100%" alignItems="Center" visible="{= ${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0 }">
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="NEWLY ADDED ACCESS (ROLE A)" class="kyraSodColHeader" /></HBox>
                                <HBox width="35%" class="kyraSodHeadCol"><Text text="NEWLY ADDED ACCESS (ROLE B)" class="kyraSodColHeader" /></HBox>
                                <HBox width="30%" class="kyraSodHeadCol"><Text text="SEGREGATION OF DUTIES (SOD) CONFLICT DETAIL" class="kyraSodColHeader" /></HBox>
                            </HBox>

                            <!-- Dynamic Conflict Rows when batch intra-role conflicts exist -->
                            <VBox items="{accessModel>/batchSodConflictsList}" visible="{= ${accessModel>/batchSodConflictsList} &amp;&amp; ${accessModel>/batchSodConflictsList}.length > 0 }">
                                <HBox class="kyraSodContentRow" alignItems="Center" width="100%">
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxRedCard1" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://customer" class="kyraSodAvatarRed sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>roleA}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>personaA}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                    <HBox width="35%" class="kyraSodColWrapper">
                                        <HBox class="kyraSodBoxRedCard2" width="100%" alignItems="Center">
                                            <Avatar src="sap-icon://database" class="kyraSodAvatarBlue sapUiTinyMarginEnd" displaySize="S" />
                                            <VBox class="kyraSodCardTextGroup">
                                                <Text text="{accessModel>roleB}" class="kyraSodBoxTitle" />
                                                <HBox alignItems="Center" class="sapUiTinyMarginTop">
                                                    <Text text="Assigned Persona: " class="kyraSodBoxSubtitleBold" />
                                                    <Text text="{accessModel>personaB}" class="kyraSodBoxSubtitle" />
                                                </HBox>
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                    <HBox width="30%" class="kyraSodColWrapper kyraSodConflictWrapper" alignItems="Center">
                                        <HBox class="kyraSodConflictDetailBoxRed" width="100%" alignItems="Center">
                                            <core:Icon src="sap-icon://alert" class="kyraSodAlertLargeIconRed sapUiTinyMarginEnd" />
                                            <VBox class="kyraSodConflictTextGroup">
                                                <Text text="{accessModel>conflictTitle}" class="kyraSodConflictMainTitleRed" />
                                                <Text text="{accessModel>conflictDesc}" class="kyraSodConflictMainDesc" />
                                            </VBox>
                                        </HBox>
                                    </HBox>
                                </HBox>
                            </VBox>

                            <!-- Clean Centered Success State Below Headings when 0 conflicts -->
                            <VBox class="kyraSodEmptyBoxCompact" visible="{= !${accessModel>/batchSodConflictsList} || ${accessModel>/batchSodConflictsList}.length === 0 }" alignItems="Center" justifyContent="Center">
                                <HBox class="kyraSodMintCircleCompact" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="sap-icon://accept" class="kyraSodMintIconCompact" />
                                </HBox>
                                <Text text="No SoD Conflicts Within Current Request" class="kyraSodEmptyTitleGreen" />
                                <Text text="The selected roles do not conflict with each other." class="kyraSodEmptyDesc" />
                            </VBox>
                        </VBox>

                        <!-- Slide 3 Footer Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Previous"
                                icon="sap-icon://navigation-left-arrow"
                                iconFirst="true"
                                type="Default"
                                press=".onGoToStep4Slide2"
                                class="kyraSecondaryBtn" />
                            <HBox alignItems="Center">
                                <Button text="Cancel" type="Transparent" press=".onCloseAddAccessSector" class="kyraTextBtn sapUiSmallMarginEnd" />
                                <Button
                                    text="Continue"
                                    type="Emphasized"
                                    icon="sap-icon://navigation-right-arrow"
                                    iconFirst="false"
                                    press=".onGoToAddAccessStep5"
                                    class="kyraPrimaryBtn" />
                            </HBox>
                        </HBox>
                    </VBox>

                    <!-- STEP 5: ACCESS REQUEST REVIEW & SUMMARY (CLEAN PROJECT-SPECIFIC SPECIFICATION) -->
                    <VBox visible="{= ${accessModel>/addAccessStep} === 5 }" class="fioriStepSlideAnim sapUiResponsivePadding">
                        <!-- Top Header Banner -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriConflictHeaderBanner sapUiSmallMarginBottom">
                            <HBox alignItems="Center">
                                <Link text="&lt; Back to SoD Conflict Review" press=".onGoToStep4Slide3" class="fioriConflictBackLink sapUiSmallMarginEnd" />
                                <Title text="Access Request Review &amp; Summary" level="H4" class="fioriBannerTitle sapUiSmallMarginBegin" />
                            </HBox>
                            <HBox alignItems="Center">
                                <Text text="Requester ID: " class="fioriRequestForLabel" />
                                <Text text="{= ${accessModel>/activeUser} || 'Dev001' }" class="fioriRequestForUser sapUiTinyMarginBegin" />
                            </HBox>
                        </HBox>

                        <!-- Top Scope & Governance Metadata Summary Card -->
                        <VBox class="kyraSummaryTopDetailsCard sapUiSmallMarginBottom">
                            <HBox wrap="Wrap" justifyContent="SpaceBetween" alignItems="Center">
                                <VBox class="kyraSummaryDetailItem">
                                    <Text text="REQUESTER ID" class="kyraSummaryDetailLabel" />
                                    <Text text="{= ${accessModel>/activeUser} || 'Dev001' }" class="kyraSummaryDetailValue" style="color: #2563EB;" />
                                </VBox>
                                <VBox class="kyraSummaryDetailItem">
                                    <Text text="BUSINESS SECTOR" class="kyraSummaryDetailLabel" />
                                    <Text text="{accessModel>/selectedSector}" class="kyraSummaryDetailValue" />
                                </VBox>
                                <VBox class="kyraSummaryDetailItem">
                                    <Text text="BUSINESS FUNCTION" class="kyraSummaryDetailLabel" />
                                    <Text text="{accessModel>/selectedFunction}" class="kyraSummaryDetailValue" />
                                </VBox>
                                <VBox class="kyraSummaryDetailItem">
                                    <Text text="OPERATING REGION" class="kyraSummaryDetailLabel" />
                                    <Text text="{accessModel>/addAccessRegion}" class="kyraSummaryDetailValue" />
                                </VBox>
                                <VBox class="kyraSummaryDetailItem">
                                    <Text text="ACCESS DURATION" class="kyraSummaryDetailLabel" />
                                    <Text text="{accessModel>/addAccessDuration}" class="kyraSummaryDetailValue" />
                                </VBox>
                            </HBox>
                            <VBox class="sapUiTinyMarginTop" visible="{= !!${accessModel>/addAccessJustification} }">
                                <Text text="BUSINESS JUSTIFICATION" class="kyraSummaryDetailLabel" />
                                <Text text="{accessModel>/addAccessJustification}" class="kyraSummaryDetailJustification" />
                            </VBox>
                        </VBox>

                        <!-- Configured Entitlements Separated Target Tables Container -->
                        <VBox items="{accessModel>/addAccessSummaryTables}">
                            <items>
                                <VBox class="kyraSummaryCardContainer">
                                    <!-- Target System Table Card Header (Matching User Template) -->
                                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="kyraSummaryCardHeader">
                                        <HBox alignItems="Center">
                                            <Avatar src="{accessModel>systemIcon}" class="kyraSummaryAvatar" displaySize="S" />
                                            <VBox>
                                                <Title text="{= 'Target System ' + ${accessModel>systemIndex} + ': ' + ${accessModel>systemName} }" level="H5" class="kyraSummaryCardTitle" />
                                                <Text text="{= ${accessModel>items}.length + ' Configured Entitlement(s)' }" class="kyraSummaryCardSubtitle" />
                                            </VBox>
                                        </HBox>
                                        <Button
                                            text="Edit Configuration"
                                            icon="sap-icon://edit"
                                            press=".onEditSystemConfiguration"
                                            class="kyraSummaryEditBtn"
                                            tooltip="Modify configuration for this target system" />
                                    </HBox>

                                    <!-- Separate 7-Column Table for this Target System (Matching User Template) -->
                                    <Table
                                        items="{accessModel>items}"
                                        noDataText="No access items configured for this system."
                                        class="kyraSummaryTable"
                                        fixedLayout="false">
                                        <columns>
                                            <!-- 1. REQUEST ID -->
                                            <Column width="16%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://document" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="REQUEST ID" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 2. TOPIC -->
                                            <Column width="15%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://comment" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="TOPIC" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 3. ROLE TITLE -->
                                            <Column width="15%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://shield" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="ROLE TITLE" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 4. ASSIGNED PERSONA -->
                                            <Column width="25%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://customer" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="ASSIGNED PERSONA" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 5. DURATION -->
                                            <Column width="11%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://calendar" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="DURATION" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 6. STATUS -->
                                            <Column width="12%">
                                                <HBox alignItems="Center">
                                                    <core:Icon src="sap-icon://message-information" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="STATUS" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                            <!-- 7. ACTION -->
                                            <Column width="6%" hAlign="Center">
                                                <HBox alignItems="Center" justifyContent="Center">
                                                    <core:Icon src="sap-icon://action-settings" class="kyraSummaryColHeaderIcon sapUiTinyMarginEnd" />
                                                    <Text text="ACTION" class="kyraSummaryColHeader" />
                                                </HBox>
                                            </Column>
                                        </columns>
                                        <items>
                                            <ColumnListItem vAlign="Middle">
                                                <cells>
                                                    <Text text="{accessModel>requestId}" class="kyraSummaryReqIdText" />
                                                    <Text text="{accessModel>topic}" class="kyraSummaryTopicText" />
                                                    <Text text="{accessModel>roleTitle}" class="kyraSummaryRoleTitleText" />
                                                    <Text text="{accessModel>persona}" class="kyraSummaryPersonaText" />
                                                    <Text text="{accessModel>duration}" class="kyraSummaryDurationText" />
                                                    <ObjectStatus
                                                        text="{accessModel>existingStatus}"
                                                        state="{accessModel>existingState}"
                                                        icon="{accessModel>existingIcon}"
                                                        class="{= 'kyraSummaryStatusText ' + (${accessModel>statusType} === 'new' ? 'kyraStatusNew' : (${accessModel>statusType} === 'pending' ? 'kyraStatusPending' : 'kyraStatusExisting')) }" />
                                                    <HBox justifyContent="Center" alignItems="Center">
                                                        <Button
                                                            icon="sap-icon://delete"
                                                            class="kyraSummaryDeleteBtn"
                                                            press=".onRemoveInPageSummaryItem"
                                                            tooltip="Remove this access item" />
                                                    </HBox>
                                                </cells>
                                            </ColumnListItem>
                                        </items>
                                    </Table>
                                </VBox>
                            </items>
                        </VBox>

                        <!-- Footer Action Bar -->
                        <HBox justifyContent="SpaceBetween" alignItems="Center" class="fioriWizardFooter sapUiSmallMarginTop" width="100%">
                            <Button
                                text="Previous"
                                icon="sap-icon://navigation-left-arrow"
                                iconFirst="true"
                                type="Default"
                                press=".onGoToStep4Slide2"
                                class="kyraSecondaryBtn" />
                            <HBox alignItems="Center">
                                <Button text="Cancel Request" type="Transparent" press=".onCloseAddAccessSector" class="kyraTextBtn sapUiSmallMarginEnd" />
                                <Button
                                    text="Submit Access Request"
                                    icon="sap-icon://navigation-right-arrow"
                                    iconFirst="false"
                                    type="Emphasized"
                                    press=".onFinalSubmitInPageAddAccess"
                                    class="kyraPrimaryBtn" />
                            </HBox>
                        </HBox>
                    </VBox>
                </VBox>

                <!-- DYNAMIC IN-PAGE REMOVE ACCESS SECTOR (Toggled via Remove Access Card) -->
                <VBox id="removeAccessSection"
                    visible="{= !!${accessModel>/showRemoveAccessSector} &amp;&amp; !${accessModel>/showRequestDetailsPage} }"
                    class="fioriTableCard sapUiResponsivePadding sapUiSmallMarginBottom fioriSlideDownAnim">
                    <HBox justifyContent="SpaceBetween" alignItems="Center" class="sapUiSmallMarginBottom">
                        <HBox alignItems="Center">
                            <Avatar src="sap-icon://delete" backgroundColor="Accent2" displaySize="S" class="sapUiTinyMarginEnd fioriCardAvatar fioriPurpleAvatar" />
                            <VBox>
                                <Title text="Revoke / Remove System Access Entitlements" level="H4" class="fioriCardHeaderTitle" />
                                <Text text="Select an active granted entitlement below to request access revocation." class="fioriDescriptionText" />
                            </VBox>
                        </HBox>
                        <Button
                            icon="sap-icon://decline"
                            type="Transparent"
                            press=".onCloseRemoveAccessSector"
                            tooltip="Close Remove Access Form" />
                    </HBox>

                    <Table
                        id="removeAccessTable"
                        items="{accessModel>/activeRoles}"
                        noDataText="No active entitlements available for removal."
                        class="sapUiNoMargin">
                        <columns>
                            <Column width="20%"><Text text="SYSTEM NAME" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="25%"><Text text="ROLE TITLE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="20%"><Text text="TEAM" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="18%"><Text text="GRANTED DATE" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                            <Column width="17%" hAlign="Center"><Text text="ACTION" style="font-weight: 700; font-size: 11px; color: #475569; letter-spacing: 0.04em;" /></Column>
                        </columns>
                        <items>
                            <ColumnListItem vAlign="Middle">
                                <cells>
                                    <Text text="{accessModel>system}" class="fioriCellBold" />
                                    <ObjectIdentifier title="{accessModel>roleName}" subtitle="{accessModel>roleId}" />
                                    <Text text="{= ${accessModel>team} || 'Enterprise Operations Team' }" />
                                    <Text text="{accessModel>grantedDate}" />
                                    <HBox justifyContent="Center">
                                        <Button
                                            text="Remove Access"
                                            icon="sap-icon://delete"
                                            type="Reject"
                                            press=".onRemoveAccessClick"
                                            tooltip="Revoke Access"
                                            class="kyraDestructiveBtn" />
                                    </HBox>
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                </VBox>

                <!-- DYNAMIC APPROVER COMPONENT (Visible ONLY when logged in as Approver or Compliance Approver and on My Access tab and Add Access is NOT active) -->
                <mvc:XMLView 
                    id="approverSectionView"
                    viewName="kyra001.pages.Approver.Approver"
                    visible="{= (${accessModel>/activeRole} === 'Approver' || ${accessModel>/activeRole} === 'Approver 1' || ${accessModel>/activeRole} === 'Approver 2' || ${accessModel>/activeRole} === 'Compliance Approver' || ${accessModel>/activeRole} === 'Compliance Reviewer' || ${accessModel>/isApproverPersona}) &amp;&amp; !${accessModel>/showAddAccessSector} &amp;&amp; !${accessModel>/showRequestDetailsPage} &amp;&amp; (${accessModel>/selectedTabKey} || 'myAccess') === 'myAccess' }" />
            </VBox>

            <!-- DEDICATED REQUEST TRACKING PAGE (Opened on clicking any row in Pending List) -->
            <VBox id="requestDetailsPageContainer"
                visible="{= !!${accessModel>/showRequestDetailsPage} }"
                class="kyraRequestDetailsPageContainer fioriStepSlideAnim">
                
                <!-- Top Breadcrumb Bar -->
                <HBox justifyContent="SpaceBetween" alignItems="Center" class="kyraReqDetailTopBar sapUiSmallMarginBottom">
                    <HBox alignItems="Center" class="kyraBreadcrumbsWrapper">
                        <core:Icon src="sap-icon://home" class="kyraBreadcrumbHomeIcon sapUiTinyMarginEnd" />
                        <Link text="Home" press=".onNavHomeFromRequestDetails" class="kyraBreadcrumbLink sapUiTinyMarginEnd" />
                        <Text text="&gt;" class="kyraBreadcrumbSep sapUiTinyMarginEnd" />
                        <Link text="Requests" press=".onBackFromRequestDetails" class="kyraBreadcrumbLink sapUiTinyMarginEnd" />
                        <Text text="&gt;" class="kyraBreadcrumbSep sapUiTinyMarginEnd" />
                        <Text text="Request Detail View" class="kyraBreadcrumbCurrent" />
                    </HBox>
                    <HBox alignItems="Center">
                        <Button icon="sap-icon://navigation-left-arrow" text="Back to Requests" type="Default" press=".onBackFromRequestDetails" class="kyraTopNavBtn" />
                    </HBox>
                </HBox>

                <!-- TOP SUMMARY CARD (Request ID | System | Role | Submitted Date) -->
                <HBox class="kyraTrackSummaryCard sapUiSmallMarginBottom" alignItems="Center">
                    <HBox class="kyraTrackDocIconBox sapUiMediumMarginEnd" justifyContent="Center" alignItems="Center">
                        <core:Icon src="sap-icon://document-text" class="kyraTrackDocIcon" />
                    </HBox>
                    
                    <HBox width="100%" justifyContent="SpaceBetween" alignItems="Center" class="kyraTrackSummaryGrid">
                        <!-- Col 1: Request ID -->
                        <VBox class="kyraTrackCol">
                            <Text text="Request ID" class="kyraTrackColLabel" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/requestId} || 'REQ-2026-000378' }" class="kyraTrackReqIdText" />
                        </VBox>

                        <!-- Col 2: System / Application -->
                        <VBox class="kyraTrackCol">
                            <Text text="System / Application" class="kyraTrackColLabel" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/system} || 'SAP S/4HANA' }" class="kyraTrackValText" />
                        </VBox>

                        <!-- Col 3: Role / Access -->
                        <VBox class="kyraTrackCol">
                            <Text text="Role / Access" class="kyraTrackColLabel" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/roleName} || 'Purchase Requisitioner' }" class="kyraTrackValText" />
                        </VBox>

                        <!-- Col 4: Submitted Date -->
                        <VBox class="kyraTrackCol kyraTrackColLast">
                            <Text text="Submitted Date" class="kyraTrackColLabel" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/submittedDateTime} || 'Aug 22, 2026 10:30 AM' }" class="kyraTrackValText" />
                        </VBox>
                    </HBox>
                </HBox>

                <!-- MAIN REQUEST TRACKING PROCESS CARD (6-Step Linear Flow) -->
                <VBox class="kyraTrackMainCard sapUiSmallMarginBottom">
                    <Title text="Request Tracking" level="H3" class="kyraTrackMainTitle" />
                    <Text text="Track the end-to-end governance and provisioning progress of your access request" class="kyraTrackMainSubtitle sapUiMediumMarginBottom" />

                    <!-- 6-STEP HORIZONTAL PROCESS TRACKER IN ONE STRAIGHT LINE -->
                    <HBox class="kyraProcessTrackerContainer" width="100%" alignItems="Start" justifyContent="SpaceBetween">
                        
                        <!-- STEP 1: Request Submitted -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLineSpacer" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step1/circleClass} || 'kyraStepCircleGreen' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step1/icon} || 'sap-icon://person-placeholder' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step1/iconClass} || 'kyraStepIconGreen' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step1/badgeClass} || 'kyraStepBadgeGreen' }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step1/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                </HBox>
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step1/lineRightClass} || 'kyraStepLineGreen' }" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step1/numBadgeClass} || 'kyraStepNumGreen' }" justifyContent="Center" alignItems="Center">
                                <Text text="1" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step1/title} || 'Request Submitted' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step1/subtitle} || 'Requester' }" class="kyraStepSubtitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step1/statusText} || 'Completed' }" class="{= ${accessModel>/selectedRequestDetail/step1/statusClass} || 'kyraStepStatusGreen' }" />
                            <Text text="{accessModel>/selectedRequestDetail/step1/time}" class="kyraStepTime" />
                        </VBox>

                        <!-- STEP 2: Approver -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step2/lineLeftClass} || 'kyraStepLineGreen' }" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step2/circleClass} || 'kyraStepCircleGreen' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step2/icon} || 'sap-icon://person-placeholder' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step2/iconClass} || 'kyraStepIconGreen' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step2/badgeClass} || 'kyraStepBadgeGreen' }" visible="{= !!${accessModel>/selectedRequestDetail/step2/showCheck} || !!${accessModel>/selectedRequestDetail/step2/showRedCross} }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step2/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                    <HBox class="kyraStepBadgeBlueDot" visible="{= !!${accessModel>/selectedRequestDetail/step2/showBlueDot} }" />
                                </HBox>
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step2/lineRightClass} || 'kyraStepLineGreen' }" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step2/numBadgeClass} || 'kyraStepNumGreen' }" justifyContent="Center" alignItems="Center">
                                <Text text="2" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step2/title} || 'Approver' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step2/subtitle} || 'Manager Approver' }" class="kyraStepSubtitle" />
                            <Text text="{accessModel>/selectedRequestDetail/step2/statusText}" class="{accessModel>/selectedRequestDetail/step2/statusClass}" visible="{= !${accessModel>/selectedRequestDetail/step2/isPill} }" />
                            <HBox class="{accessModel>/selectedRequestDetail/step2/pillClass}" visible="{= !!${accessModel>/selectedRequestDetail/step2/isPill} }" justifyContent="Center" alignItems="Center">
                                <Text text="{accessModel>/selectedRequestDetail/step2/statusText}" class="{accessModel>/selectedRequestDetail/step2/pillTextClass}" />
                            </HBox>
                            <Text text="{accessModel>/selectedRequestDetail/step2/subNote}" class="kyraStepSubNote" visible="{= !!${accessModel>/selectedRequestDetail/step2/subNote} }" />
                            <Text text="{accessModel>/selectedRequestDetail/step2/time}" class="kyraStepTime" visible="{= !${accessModel>/selectedRequestDetail/step2/isPill} }" />
                        </VBox>

                        <!-- STEP 3: Compliance Approver -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step3/lineLeftClass} || 'kyraStepLineGreen' }" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step3/circleClass} || 'kyraStepCircleBlue' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step3/icon} || 'sap-icon://shield' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step3/iconClass} || 'kyraStepIconBlue' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step3/badgeClass} || 'kyraStepBadgeGreen' }" visible="{= !!${accessModel>/selectedRequestDetail/step3/showCheck} || !!${accessModel>/selectedRequestDetail/step3/showRedCross} }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step3/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                    <HBox class="kyraStepBadgeBlueDot" visible="{= !!${accessModel>/selectedRequestDetail/step3/showBlueDot} }" />
                                </HBox>
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step3/lineRightClass} || 'kyraStepLineDashed' }" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step3/numBadgeClass} || 'kyraStepNumBlue' }" justifyContent="Center" alignItems="Center">
                                <Text text="3" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step3/title} || 'Compliance Approver' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step3/subtitle} || 'Compliance Review' }" class="kyraStepSubtitle" />
                            <Text text="{accessModel>/selectedRequestDetail/step3/statusText}" class="{accessModel>/selectedRequestDetail/step3/statusClass}" visible="{= !${accessModel>/selectedRequestDetail/step3/isPill} }" />
                            <HBox class="{accessModel>/selectedRequestDetail/step3/pillClass}" visible="{= !!${accessModel>/selectedRequestDetail/step3/isPill} }" justifyContent="Center" alignItems="Center">
                                <Text text="{accessModel>/selectedRequestDetail/step3/statusText}" class="{accessModel>/selectedRequestDetail/step3/pillTextClass}" />
                            </HBox>
                            <Text text="{accessModel>/selectedRequestDetail/step3/subNote}" class="kyraStepSubNote" visible="{= !!${accessModel>/selectedRequestDetail/step3/subNote} }" />
                            <Text text="{accessModel>/selectedRequestDetail/step3/time}" class="kyraStepTime" visible="{= !${accessModel>/selectedRequestDetail/step3/isPill} }" />
                        </VBox>

                        <!-- STEP 4: IAM Approver -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step4/lineLeftClass} || 'kyraStepLineDashed' }" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step4/circleClass} || 'kyraStepCircleGray' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step4/icon} || 'sap-icon://user-settings' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step4/iconClass} || 'kyraStepIconGray' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step4/badgeClass} || 'kyraStepBadgeGreen' }" visible="{= !!${accessModel>/selectedRequestDetail/step4/showCheck} || !!${accessModel>/selectedRequestDetail/step4/showRedCross} }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step4/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                    <HBox class="kyraStepBadgeBlueDot" visible="{= !!${accessModel>/selectedRequestDetail/step4/showBlueDot} }" />
                                </HBox>
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step4/lineRightClass} || 'kyraStepLineDashed' }" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step4/numBadgeClass} || 'kyraStepNumGray' }" justifyContent="Center" alignItems="Center">
                                <Text text="4" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step4/title} || 'IAM Approver' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step4/subtitle} || 'Identity Governance' }" class="kyraStepSubtitle" />
                            <Text text="{accessModel>/selectedRequestDetail/step4/statusText}" class="{accessModel>/selectedRequestDetail/step4/statusClass}" visible="{= !${accessModel>/selectedRequestDetail/step4/isPill} }" />
                            <HBox class="{accessModel>/selectedRequestDetail/step4/pillClass}" visible="{= !!${accessModel>/selectedRequestDetail/step4/isPill} }" justifyContent="Center" alignItems="Center">
                                <Text text="{accessModel>/selectedRequestDetail/step4/statusText}" class="{accessModel>/selectedRequestDetail/step4/pillTextClass}" />
                            </HBox>
                            <Text text="{accessModel>/selectedRequestDetail/step4/subNote}" class="kyraStepSubNote" visible="{= !!${accessModel>/selectedRequestDetail/step4/subNote} }" />
                            <Text text="{accessModel>/selectedRequestDetail/step4/time}" class="kyraStepTime" visible="{= !${accessModel>/selectedRequestDetail/step4/isPill} }" />
                        </VBox>

                        <!-- STEP 5: IAM Approver 2 -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step5/lineLeftClass} || 'kyraStepLineDashed' }" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step5/circleClass} || 'kyraStepCircleGray' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step5/icon} || 'sap-icon://sys-monitor' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step5/iconClass} || 'kyraStepIconGray' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step5/badgeClass} || 'kyraStepBadgeGreen' }" visible="{= !!${accessModel>/selectedRequestDetail/step5/showCheck} || !!${accessModel>/selectedRequestDetail/step5/showRedCross} }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step5/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                    <HBox class="kyraStepBadgeBlueDot" visible="{= !!${accessModel>/selectedRequestDetail/step5/showBlueDot} }" />
                                </HBox>
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step5/lineRightClass} || 'kyraStepLineDashed' }" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step5/numBadgeClass} || 'kyraStepNumGray' }" justifyContent="Center" alignItems="Center">
                                <Text text="5" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step5/title} || 'IAM Approver 2' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step5/subtitle} || 'Access Provisioning' }" class="kyraStepSubtitle" />
                            <Text text="{accessModel>/selectedRequestDetail/step5/statusText}" class="{accessModel>/selectedRequestDetail/step5/statusClass}" visible="{= !${accessModel>/selectedRequestDetail/step5/isPill} }" />
                            <HBox class="{accessModel>/selectedRequestDetail/step5/pillClass}" visible="{= !!${accessModel>/selectedRequestDetail/step5/isPill} }" justifyContent="Center" alignItems="Center">
                                <Text text="{accessModel>/selectedRequestDetail/step5/statusText}" class="{accessModel>/selectedRequestDetail/step5/pillTextClass}" />
                            </HBox>
                            <Text text="{accessModel>/selectedRequestDetail/step5/subNote}" class="kyraStepSubNote" visible="{= !!${accessModel>/selectedRequestDetail/step5/subNote} }" />
                            <Text text="{accessModel>/selectedRequestDetail/step5/time}" class="kyraStepTime" visible="{= !${accessModel>/selectedRequestDetail/step5/isPill} }" />
                        </VBox>

                        <!-- STEP 6: Completed -->
                        <VBox class="kyraTrackStepBox" alignItems="Center">
                            <HBox class="kyraStepIconRow" alignItems="Center" width="100%">
                                <HBox class="kyraStepLine {= ${accessModel>/selectedRequestDetail/step6/lineLeftClass} || 'kyraStepLineDashed' }" />
                                <HBox class="kyraStepCircle {= ${accessModel>/selectedRequestDetail/step6/circleClass} || 'kyraStepCircleGray' }" justifyContent="Center" alignItems="Center">
                                    <core:Icon src="{= ${accessModel>/selectedRequestDetail/step6/icon} || 'sap-icon://flag' }" class="kyraStepIcon {= ${accessModel>/selectedRequestDetail/step6/iconClass} || 'kyraStepIconGray' }" />
                                    <HBox class="{= ${accessModel>/selectedRequestDetail/step6/badgeClass} || 'kyraStepBadgeGreen' }" visible="{= !!${accessModel>/selectedRequestDetail/step6/showCheck} || !!${accessModel>/selectedRequestDetail/step6/showRedCross} }" justifyContent="Center" alignItems="Center">
                                        <core:Icon src="{= ${accessModel>/selectedRequestDetail/step6/checkIcon} || 'sap-icon://accept' }" class="kyraBadgeCheckIcon" />
                                    </HBox>
                                </HBox>
                                <HBox class="kyraStepLineSpacer" />
                            </HBox>
                            <HBox class="kyraStepNumBadge {= ${accessModel>/selectedRequestDetail/step6/numBadgeClass} || 'kyraStepNumGray' }" justifyContent="Center" alignItems="Center">
                                <Text text="6" class="kyraStepNumText" />
                            </HBox>
                            <Text text="{= ${accessModel>/selectedRequestDetail/step6/title} || 'Completed' }" class="kyraStepTitle" />
                            <Text text="{= ${accessModel>/selectedRequestDetail/step6/subtitle} || 'Access Activated' }" class="kyraStepSubtitle" />
                            <Text text="{accessModel>/selectedRequestDetail/step6/statusText}" class="{accessModel>/selectedRequestDetail/step6/statusClass}" visible="{= !${accessModel>/selectedRequestDetail/step6/isPill} }" />
                            <HBox class="{accessModel>/selectedRequestDetail/step6/pillClass}" visible="{= !!${accessModel>/selectedRequestDetail/step6/isPill} }" justifyContent="Center" alignItems="Center">
                                <Text text="{accessModel>/selectedRequestDetail/step6/statusText}" class="{accessModel>/selectedRequestDetail/step6/pillTextClass}" />
                            </HBox>
                            <Text text="{accessModel>/selectedRequestDetail/step6/subNote}" class="kyraStepSubNote" visible="{= !!${accessModel>/selectedRequestDetail/step6/subNote} }" />
                            <Text text="{accessModel>/selectedRequestDetail/step6/time}" class="kyraStepTime" visible="{= !${accessModel>/selectedRequestDetail/step6/isPill} }" />
                        </VBox>
                    </HBox>
                </VBox>

                <!-- COMMENTS / REMARKS SECTION (Directly Below Tracking Section) -->
                <VBox class="kyraTrackCommentsCard sapUiMediumMarginBottom">
                    <!-- Header with Title on Left -->
                    <HBox alignItems="Center" width="100%" class="kyraCommentsCardHeader sapUiSmallMarginBottom">
                        <HBox class="kyraTrackInfoIconCircle sapUiSmallMarginEnd" justifyContent="Center" alignItems="Center">
                            <core:Icon src="sap-icon://comment" class="kyraTrackInfoIcon" />
                        </HBox>
                        <VBox>
                            <Title text="Comments / Remarks" level="H4" class="kyraCommentsTitle" />
                            <Text text="Governance activity log and stakeholder remarks" class="kyraCommentsSub" />
                        </VBox>
                    </HBox>

                    <!-- Comments List Stream -->
                    <VBox class="kyraCommentListContainer" items="{accessModel>/selectedRequestDetail/activityLogs}">
                        <HBox class="kyraCommentItemBox" alignItems="Start">
                            <core:Icon src="{accessModel>icon}" class="{accessModel>iconClass} sapUiSmallMarginEnd" />
                            <VBox width="100%">
                                <HBox justifyContent="SpaceBetween" alignItems="Center" width="100%">
                                    <Text text="{accessModel>title}" style="font-weight: 700; font-size: 13px; color: #0F172A;" />
                                    <Text text="{accessModel>date}" style="font-size: 11.5px; color: #94A3B8;" />
                                </HBox>
                                <Text text="{accessModel>comment}" style="font-size: 12.5px; color: #475569; margin-top: 4px; line-height: 1.4;" />
                                <Text text="{accessModel>author}" style="font-size: 11px; color: #64748B; margin-top: 4px; font-style: italic;" />
                            </VBox>
                        </HBox>
                    </VBox>
                </VBox>
            </VBox>
            </VBox>
        </content>
    </Page>
</mvc:View>
