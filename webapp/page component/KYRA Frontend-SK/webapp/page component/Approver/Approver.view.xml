<mvc:View
    controllerName="kyra001.pages.Approver.Approver"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core">

    <VBox class="sapUiNoContentPadding">

        <!-- SECTION 2: USER REQUESTS BOX &amp; HISTORY LOG -->
        <VBox
            visible="true"
            class="kyraApproverCard sapUiNoMargin">
            
            <!-- ROW 1: HEADER (Left: Avatar, Title, Subtitle with teal bar | Right: Solid Kyra Teal Segmented Pill Buttons) -->
            <HBox justifyContent="SpaceBetween" alignItems="Center" wrap="Wrap" class="kyraApproverTopRow">
                <!-- LEFT: Avatar + Title & Subtitle with vertical accent bar -->
                <HBox alignItems="Center" class="kyraCardHeaderLeft">
                    <Avatar src="sap-icon://user-edit" class="kyraMasterViewAvatar kyraSquircleAvatar sapUiSmallMarginEnd" displaySize="S" />
                    <VBox class="kyraCardHeaderTextCol">
                        <Title text="{= ${accessModel>/showApprovalHistory} ? 'Processed Approval History Log' : (${accessModel>/isCompliance} ? 'Compliance Review Requests' : 'User Requests') }" level="H4" class="fioriApproverTitle kyraCardHeaderTitle" />
                        <Text text="{= ${accessModel>/showApprovalHistory} ? 'Audit history of all previously processed governance compliance reviews.' : (${accessModel>/isCompliance} ? 'Review access requests requiring Segregation of Duties (SoD) compliance clearance.' : 'Select any request row below to open the dedicated full summary review page.') }" class="fioriDescriptionText kyraSubtitleWithTealBar" />
                    </VBox>
                </HBox>

                <!-- RIGHT: Pending (7) and History (7) Pill Buttons (Solid Kyra Teal #008C9C on selected) -->
                <HBox class="kyraApproverPillGroup" alignItems="Center">
                    <Button
                        icon="sap-icon://time-entry-request"
                        text="{= 'Pending (' + ((${accessModel>/pendingRequests} || []).length) + ')' }"
                        type="{= !${accessModel>/showApprovalHistory} ? 'Emphasized' : 'Transparent' }"
                        press=".onSelectPendingQueue"
                        tooltip="Pending Queue"
                        class="kyraApproverPillBtn" />
                    <Button
                        icon="sap-icon://history"
                        text="{= 'History (' + ((${accessModel>/processedRequests} || []).length) + ')' }"
                        type="{= ${accessModel>/showApprovalHistory} ? 'Emphasized' : 'Transparent' }"
                        press=".onSelectHistoryLog"
                        tooltip="Processed History"
                        class="kyraApproverPillBtn" />
                </HBox>
            </HBox>

            <!-- ROW 2: SUB-TOOLBAR (Left: Access & Revoke Segmented tabs | Right: Search, Filter, Export) -->
            <HBox justifyContent="SpaceBetween" alignItems="Center" wrap="Wrap" class="kyraApproverSubToolbar">
                <!-- LEFT: Access & Revoke tab buttons (Solid Kyra Teal #008C9C on selected) -->
                <HBox visible="{= !${accessModel>/isCompliance} || !${accessModel>/showApprovalHistory} }" class="kyraAccessRevokeBar" alignItems="Center">
                    <Button
                        text="{= 'Access (' + (${accessModel>/showApprovalHistory} ? (${accessModel>/historyAccessCount} || 0) : (${accessModel>/pendingAccessCount} || 0)) + ')' }"
                        icon="sap-icon://key"
                        type="{= (${accessModel>/showApprovalHistory} ? (${accessModel>/approverHistoryTab} || 'accessRequests') : (${accessModel>/approverPendingTab} || 'accessRequests')) === 'accessRequests' ? 'Emphasized' : 'Transparent' }"
                        press=".onSelectAccessRequestsTab"
                        tooltip="{= ${accessModel>/showApprovalHistory} ? 'History Access Requests' : 'Pending Access Requests' }"
                        class="kyraTabPillBtn" />
                    <Button
                        visible="{= !${accessModel>/isCompliance} }"
                        text="{= 'Revoke (' + (${accessModel>/showApprovalHistory} ? (${accessModel>/historyRevokeCount} || 0) : (${accessModel>/pendingRevokeCount} || 0)) + ')' }"
                        icon="sap-icon://delete"
                        type="{= (${accessModel>/showApprovalHistory} ? (${accessModel>/approverHistoryTab} || 'accessRequests') : (${accessModel>/approverPendingTab} || 'accessRequests')) === 'revokeRequests' ? 'Emphasized' : 'Transparent' }"
                        press=".onSelectRevokeRequestsTab"
                        tooltip="{= ${accessModel>/showApprovalHistory} ? 'History Revocation Requests' : 'Pending Revocation Requests' }"
                        class="kyraTabPillBtn" />
                </HBox>

                <!-- RIGHT: Search, Filter, Export Controls -->
                <HBox alignItems="Center" class="kyraApproverRightControls">
                    <SearchField
                        placeholder="Search request..."
                        width="210px"
                        liveChange=".onSearchApprovalTable"
                        class="kyraEntitlementsSearchField" />
                    
                    <Button
                        icon="sap-icon://filter"
                        type="Transparent"
                        press=".onFilterApprovalsDialog"
                        tooltip="Filter Requests"
                        class="kyraHistoryExportBtn" />

                    <Button
                        icon="sap-icon://excel-attachment"
                        type="Transparent"
                        press=".onExportApprovals"
                        tooltip="Export Audit Log"
                        class="kyraHistoryExportBtn" />
                </HBox>
            </HBox>

            <!-- CLASSIFICATIONS CONTENT -->
            <VBox visible="{= !${accessModel>/showApprovalHistory} }" class="sapUiNoMargin">

                <!-- CLASSIFICATION 1: ACCESS REQUESTS -->
                <Table
                    id="approvalAccessTable"
                    visible="{= (${accessModel>/approverPendingTab} || 'accessRequests') === 'accessRequests' }"
                    items="{accessModel>/pendingAccessRequests}"
                    noDataText="No pending access requests in queue."
                    fixedLayout="true"
                    popinLayout="GridSmall"
                    class="sapUiNoMargin kyraApproverTable">
                    <columns>
                        <Column width="10%" hAlign="Center"><Text text="USER ID" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="22%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS SECTOR" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                        <Column width="20%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS FUNCTION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                        <Column width="14%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="DURATION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="16%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="SUBMISSION DATE" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="18%" hAlign="Center"><Text text="APPROVAL STATUS" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    </columns>
                    <items>
                        <ColumnListItem type="Navigation" highlight="None" press=".onOpenRequestSummaryDialog" vAlign="Middle" class="kyraApproverRowItem kyraTableRow">
                            <cells>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{accessModel>requesterId}" wrapping="false" class="kyraUserIdText kyraCellTextCenter" />
                                </HBox>
                                <Text text="{= (${accessModel>businessSector} &amp;&amp; ${accessModel>businessSector} !== '—' &amp;&amp; ${accessModel>businessSector} !== '–' &amp;&amp; ${accessModel>businessSector} !== '-') ? ${accessModel>businessSector} : ((${accessModel>sector} &amp;&amp; ${accessModel>sector} !== '—' &amp;&amp; ${accessModel>sector} !== '–' &amp;&amp; ${accessModel>sector} !== '-') ? ${accessModel>sector} : 'Information Technology &amp; Security') }" wrapping="true" class="kyraSectorText kyraCellText kyraCellTextLeft" />
                                <Text text="{= (${accessModel>businessFunction} &amp;&amp; ${accessModel>businessFunction} !== '—' &amp;&amp; ${accessModel>businessFunction} !== '–' &amp;&amp; ${accessModel>businessFunction} !== '-') ? ${accessModel>businessFunction} : ((${accessModel>function} &amp;&amp; ${accessModel>function} !== '—' &amp;&amp; ${accessModel>function} !== '–' &amp;&amp; ${accessModel>function} !== '-') ? ${accessModel>function} : 'Corporate Governance') }" wrapping="true" class="kyraFunctionText kyraCellText kyraCellTextLeft" />
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{= (${accessModel>duration} &amp;&amp; ${accessModel>duration} !== '—' &amp;&amp; ${accessModel>duration} !== '–' &amp;&amp; ${accessModel>duration} !== '-') ? ${accessModel>duration} : ((${accessModel>accessDuration} &amp;&amp; ${accessModel>accessDuration} !== '—' &amp;&amp; ${accessModel>accessDuration} !== '–' &amp;&amp; ${accessModel>accessDuration} !== '-') ? ${accessModel>accessDuration} : 'Permanent') }" wrapping="false" class="kyraCellTextCenter kyraDurationCell" />
                                </HBox>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{accessModel>submissionDate}" wrapping="false" class="kyraDateText kyraCellTextCenter kyraDateCell" />
                                </HBox>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <ObjectStatus text="{accessModel>status}" state="{accessModel>statusState}" icon="{= ${accessModel>statusIcon} || (${accessModel>status} === 'Approved' || ${accessModel>status} === 'Active' ? 'sap-icon://sys-enter-2' : (${accessModel>status} === 'Rejected' ? 'sap-icon://error' : 'sap-icon://history')) }" class="kyraTableStatusBadge kyraApproverStatusBadge" />
                                </HBox>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>

                <!-- CLASSIFICATION 2: REVOCATION REQUESTS -->
                <Table
                    id="approvalRevokeTable"
                    visible="{= !${accessModel>/isCompliance} &amp;&amp; ${accessModel>/approverPendingTab} === 'revokeRequests' }"
                    items="{accessModel>/pendingRevokeRequests}"
                    noDataText="No pending revocation requests in queue."
                    fixedLayout="true"
                    popinLayout="GridSmall"
                    class="sapUiNoMargin kyraApproverTable">
                    <columns>
                        <Column width="10%" hAlign="Center"><Text text="USER ID" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="22%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS SECTOR" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                        <Column width="20%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS FUNCTION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                        <Column width="14%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="A/R Duration" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="16%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="SUBMISSION DATE" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                        <Column width="18%" hAlign="Center"><Text text="APPROVAL STATUS" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    </columns>
                    <items>
                        <ColumnListItem type="Navigation" highlight="None" press=".onOpenRequestSummaryDialog" vAlign="Middle" class="kyraApproverRowItem kyraTableRow">
                            <cells>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{accessModel>requesterId}" wrapping="false" class="kyraUserIdText kyraCellTextCenter" />
                                </HBox>
                                <Text text="{= (${accessModel>businessSector} &amp;&amp; ${accessModel>businessSector} !== '—' &amp;&amp; ${accessModel>businessSector} !== '–' &amp;&amp; ${accessModel>businessSector} !== '-') ? ${accessModel>businessSector} : ((${accessModel>sector} &amp;&amp; ${accessModel>sector} !== '—' &amp;&amp; ${accessModel>sector} !== '–' &amp;&amp; ${accessModel>sector} !== '-') ? ${accessModel>sector} : 'Information Technology &amp; Security') }" wrapping="true" class="kyraSectorText kyraCellText kyraCellTextLeft" />
                                <Text text="{= (${accessModel>businessFunction} &amp;&amp; ${accessModel>businessFunction} !== '—' &amp;&amp; ${accessModel>businessFunction} !== '–' &amp;&amp; ${accessModel>businessFunction} !== '-') ? ${accessModel>businessFunction} : ((${accessModel>function} &amp;&amp; ${accessModel>function} !== '—' &amp;&amp; ${accessModel>function} !== '–' &amp;&amp; ${accessModel>function} !== '-') ? ${accessModel>function} : 'Corporate Governance') }" wrapping="true" class="kyraFunctionText kyraCellText kyraCellTextLeft" />
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{= (${accessModel>duration} &amp;&amp; ${accessModel>duration} !== '—' &amp;&amp; ${accessModel>duration} !== '–' &amp;&amp; ${accessModel>duration} !== '-') ? ${accessModel>duration} : ((${accessModel>accessDuration} &amp;&amp; ${accessModel>accessDuration} !== '—' &amp;&amp; ${accessModel>accessDuration} !== '–' &amp;&amp; ${accessModel>accessDuration} !== '-') ? ${accessModel>accessDuration} : 'Permanent') }" wrapping="false" class="kyraCellTextCenter kyraDurationCell" />
                                </HBox>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <Text text="{accessModel>submissionDate}" wrapping="false" class="kyraDateText kyraCellTextCenter kyraDateCell" />
                                </HBox>
                                <HBox justifyContent="Center" alignItems="Center" width="100%">
                                    <ObjectStatus text="{accessModel>status}" state="{accessModel>statusState}" icon="{= ${accessModel>statusIcon} || (${accessModel>status} === 'Approved' || ${accessModel>status} === 'Active' ? 'sap-icon://sys-enter-2' : (${accessModel>status} === 'Rejected' ? 'sap-icon://error' : 'sap-icon://history')) }" class="kyraTableStatusBadge kyraApproverStatusBadge" />
                                </HBox>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </VBox>

            <!-- SECTION 3: PROCESSED APPROVAL HISTORY LOG -->
            <Table
                id="approvalHistoryTable"
                visible="{= ${accessModel>/showApprovalHistory} === true }"
                items="{accessModel>/displayedHistoryRequests}"
                noDataText="{= ${accessModel>/isCompliance} ? 'No processed approval history available.' : ((${accessModel>/approverHistoryTab} || 'accessRequests') === 'revokeRequests' ? 'No processed revocation history available.' : 'No processed access history available.') }"
                fixedLayout="true"
                popinLayout="GridSmall"
                class="sapUiNoMargin kyraApproverTable">
                <columns>
                    <Column width="10%" hAlign="Center"><Text text="USER ID" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    <Column width="21%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS SECTOR" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                    <Column width="21%" minScreenWidth="Tablet" demandPopin="true" hAlign="Begin"><Text text="BUSINESS FUNCTION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadLeft" /></Column>
                    <Column width="13%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="DURATION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    <Column width="13%" minScreenWidth="Desktop" demandPopin="true" hAlign="Center"><Text text="DECISION DATE" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    <Column width="12%" hAlign="Center"><Text text="FINAL DECISION" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                    <Column width="10%" hAlign="Center"><Text text="BREAKDOWN" wrapping="false" class="kyraEntitlementColHeader kyraColHeadCenter" /></Column>
                </columns>
                <items>
                    <ColumnListItem highlight="None" vAlign="Middle" class="kyraApproverRowItem kyraTableRow">
                        <cells>
                            <HBox justifyContent="Center" alignItems="Center" width="100%">
                                <Text text="{accessModel>requesterId}" wrapping="false" class="kyraUserIdText kyraCellTextCenter" />
                            </HBox>
                            <Text text="{= (${accessModel>businessSector} &amp;&amp; ${accessModel>businessSector} !== '—' &amp;&amp; ${accessModel>businessSector} !== '–' &amp;&amp; ${accessModel>businessSector} !== '-') ? ${accessModel>businessSector} : ((${accessModel>sector} &amp;&amp; ${accessModel>sector} !== '—' &amp;&amp; ${accessModel>sector} !== '–' &amp;&amp; ${accessModel>sector} !== '-') ? ${accessModel>sector} : 'Information Technology &amp; Security') }" wrapping="true" class="kyraSectorText kyraCellText kyraCellTextLeft" />
                            <Text text="{= (${accessModel>businessFunction} &amp;&amp; ${accessModel>businessFunction} !== '—' &amp;&amp; ${accessModel>businessFunction} !== '–' &amp;&amp; ${accessModel>businessFunction} !== '-') ? ${accessModel>businessFunction} : ((${accessModel>function} &amp;&amp; ${accessModel>function} !== '—' &amp;&amp; ${accessModel>function} !== '–' &amp;&amp; ${accessModel>function} !== '-') ? ${accessModel>function} : 'Corporate Governance') }" wrapping="true" class="kyraFunctionText kyraCellText kyraCellTextLeft" />
                            <HBox justifyContent="Center" alignItems="Center" width="100%">
                                <Text text="{= (${accessModel>duration} &amp;&amp; ${accessModel>duration} !== '—' &amp;&amp; ${accessModel>duration} !== '–' &amp;&amp; ${accessModel>duration} !== '-') ? ${accessModel>duration} : ((${accessModel>accessDuration} &amp;&amp; ${accessModel>accessDuration} !== '—' &amp;&amp; ${accessModel>accessDuration} !== '–' &amp;&amp; ${accessModel>accessDuration} !== '-') ? ${accessModel>accessDuration} : 'Permanent') }" wrapping="false" class="kyraCellTextCenter kyraDurationCell" />
                            </HBox>
                            <HBox justifyContent="Center" alignItems="Center" width="100%">
                                <Text text="{accessModel>decisionDate}" wrapping="false" class="kyraDateText kyraCellTextCenter kyraDateCell" />
                            </HBox>
                            <HBox justifyContent="Center" alignItems="Center" width="100%">
                                <ObjectStatus
                                    text="{accessModel>status}"
                                    state="{accessModel>statusState}"
                                    icon="{= ${accessModel>statusIcon} || (${accessModel>status} === 'Approved' || ${accessModel>status} === 'Active' ? 'sap-icon://sys-enter-2' : (${accessModel>status} === 'Rejected' ? 'sap-icon://error' : 'sap-icon://history')) }"
                                    class="kyraTableStatusBadge kyraApproverStatusBadge" />
                            </HBox>
                            <HBox justifyContent="Center" alignItems="Center" width="100%">
                                <Button
                                    text="View"
                                    icon="sap-icon://hint"
                                    type="Transparent"
                                    press=".onOpenDecisionBreakdownDialog"
                                    tooltip="View Approved and Rejected Details"
                                    class="kyraBreakdownViewBtn kyraTableActionBtn" />
                            </HBox>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </VBox>
    </VBox>
</mvc:View>
